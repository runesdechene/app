# V0.7 — Système de Niveaux & Refonte Gloire — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer la jauge "Gloire" par un système Niveau (cap 50) acquis à vie, avec coupure nette pour tous les joueurs et badge "Vétéran de la Première Époque" pour les anciens.

**Architecture:** Tout repose sur un compteur DB `users.xp_total` incrémenté/décrémenté par triggers sur 5 tables d'action (avec garde `xp_epoch` pour ignorer l'historique). Le niveau est calculé à la volée par `_level_from_xp()`. Les RPCs d'action enrichissent leur retour avec `{xp_delta, level_before, level_after}` pour permettre au front de déclencher la modale level up. La Gloire devient un mot lyrique pour l'XP, plus une jauge séparée. Les titres généraux passent en threshold acquis à vie sur 7 axes.

**Tech Stack:** Supabase (PostgreSQL + Triggers + RPCs SECURITY DEFINER), React 18 + Vite + TypeScript strict + Zustand 5 + MapLibre. Pas de runner de tests JS dans la repo : vérifications via `pnpm dev` + assertions SQL manuelles dans Supabase Studio.

**Spec source :** `docs/superpowers/specs/2026-05-01-v07-niveaux-design.md`

---

## File Structure

### Migrations SQL (à créer)

| Fichier | Responsabilité |
|---|---|
| `supabase/migrations/040_v07_levels_schema.sql` | ALTER TABLE users (xp_total, veteran_first_era), app_config xp_epoch, function `_level_from_xp` |
| `supabase/migrations/041_v07_levels_triggers.sql` | Triggers INSERT/DELETE sur 5 tables d'action |
| `supabase/migrations/042_v07_levels_titles_refonte.sql` | DELETE Bâtisseur + UPDATE titres existants + INSERT nouveaux (33 finaux) |
| `supabase/migrations/043_v07_levels_get_user_titles.sql` | Refonte `get_user_titles` pour évaluer sur compteurs étendus |
| `supabase/migrations/044_v07_levels_player_profile.sql` | Refonte `get_player_profile` (level/xp_total/xpToNextLevel/veteran_first_era, plus de glory) |
| `supabase/migrations/045_v07_levels_leaderboard.sql` | Refonte `get_leaderboard('notoriety')` tri par level |
| `supabase/migrations/046_v07_levels_action_rpcs.sql` | Enrichissement des RPCs d'action (xp_delta, level_before, level_after) + gating cartographier niveau 3 |
| `supabase/migrations/047_v07_levels_veteran_marking.sql` | UPDATE veteran_first_era pour les contributeurs historiques (à appliquer juste avant déploiement front) |
| `supabase/migrations/048_v07_drop_glory_legacy.sql` | **Différée ~2 semaines post-déploiement.** DROP `get_my_glory`, colonnes `exploration_points`/`erudition_points` |

### Frontend (à créer/modifier)

| Fichier | Action | Responsabilité |
|---|---|---|
| `apps/explore-web/src/types/level.ts` | Créer | Types `LevelState`, `LevelDelta`, helpers `xpForLevel(N)` |
| `apps/explore-web/src/hooks/useLevel.ts` | Créer | Hook lecture niveau + xpTotal + xpToNextLevel via `get_my_informations` (étendue) |
| `apps/explore-web/src/hooks/useLevelUp.ts` | Créer | Hook subscription level up events + déclenche modale |
| `apps/explore-web/src/stores/playerStore.ts` | Modifier | Ajouter `level`, `xpTotal`, `xpToNextLevel`, `veteranFirstEra` |
| `apps/explore-web/src/hooks/usePlayer.ts` | Modifier | Refonte toasts (nouveaux wordings), retirer le bug "+1 Gloire +1 Coupe" trompeur |
| `apps/explore-web/src/components/profile/LevelMedallion.tsx` + `.css` | Créer | Médaillon sceau de chevalier avec niveau au centre |
| `apps/explore-web/src/components/profile/VeteranBadge.tsx` + `.css` | Créer | Badge "Vétéran de la Première Époque" |
| `apps/explore-web/src/components/profile/GloryProgressBar.tsx` + `.css` | Créer | Barre de progression "X Gloire avant le prochain niveau" |
| `apps/explore-web/src/components/profile/PlayerProfilePanel.tsx` (chemin à confirmer) | Modifier | Intégrer médaillon + badge + barre, retirer affichage Gloire brute |
| `apps/explore-web/src/components/levelup/LevelUpModal.tsx` + `.css` | Créer | Modale célébrative single + multi-niveau |
| `apps/explore-web/src/components/levelup/VeteranWelcomeModal.tsx` + `.css` | Créer | Modale dédiée premier login post-switch (vétérans) |
| `apps/explore-web/src/components/places/AddPlaceFlow.tsx` | Modifier | Refonte des toasts récompenses + check niveau >= 3 (appel RPC level-aware) |
| `apps/explore-web/src/components/places/AddPlaceButton.tsx` (chemin à confirmer) | Modifier | Visuel grisé + tooltip si niveau < 3 |
| `apps/explore-web/src/lib/levelCalc.ts` | Créer | Fonctions pures `xpForLevel(N)` et `levelFromXp(xp)` (mirror du SQL `_level_from_xp`) |

### Documentation à mettre à jour

| Fichier | Action |
|---|---|
| `apps/explore-web/CLAUDE.md` | Ajouter mention système niveaux V0.7, vocabulaire Découvrir/Fouler/Cartographier |
| `CLAUDE.md` (racine) | Mention rapide phase niveaux dans l'écosystème |
| Changelog public à préparer (chemin selon convention RdC) | À écrire avant déploiement |

---

## Phase 1 — Schéma & function `_level_from_xp`

### Task 1: Migration schéma `040_v07_levels_schema.sql`

**Files:**
- Create: `supabase/migrations/040_v07_levels_schema.sql`

- [ ] **Step 1: Créer le fichier migration avec le schéma**

```sql
-- 040_v07_levels_schema.sql
-- WHY : Phase 1 du système Niveaux V0.7.
--   - Ajoute xp_total (compteur effort cumulé post-epoch, jamais affiché brut)
--   - Ajoute veteran_first_era (badge attribué aux contributeurs pré-switch)
--   - Stocke xp_epoch dans app_config (date du switch, secondes UTC)
--   - Crée _level_from_xp (function pure pour dériver le niveau)
--
-- Cette mig est SAFE : aucun calcul ni trigger encore. Juste le schéma.

ALTER TABLE public.users
  ADD COLUMN IF NOT EXISTS xp_total integer NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS veteran_first_era boolean NOT NULL DEFAULT false;

-- xp_epoch = timestamp d'application de cette mig (utilisé par les triggers
-- pour ignorer l'historique pré-switch)
INSERT INTO public.app_config (key, value)
VALUES ('xp_epoch', extract(epoch from now())::text)
ON CONFLICT (key) DO NOTHING;

-- Function pure : XP cumulée → Niveau (cap 50).
-- Régime onboarding (niv 1-3 hardcodé) :
--   1→2 = 5 XP, 2→3 = 8 XP, donc cumul atteint :
--   niv 1 = 0 XP, niv 2 = 5 XP, niv 3 = 13 XP
-- Régime sérieux (niv 3+) : coût(N→N+1) = 25 × 1.05^(N-3)
--   cumul(N) = 13 + 500 × (1.05^(N-3) - 1)
--   inversion : N = 3 + floor(ln(1 + (xp - 13) / 500) / ln(1.05))
CREATE OR REPLACE FUNCTION public._level_from_xp(p_xp integer)
RETURNS integer LANGUAGE sql IMMUTABLE AS $$
  SELECT CASE
    WHEN p_xp IS NULL OR p_xp < 5  THEN 1
    WHEN p_xp < 13                  THEN 2
    WHEN p_xp < 38                  THEN 3
    ELSE LEAST(50, 3 + FLOOR(LN(1 + (p_xp - 13)::numeric / 500) / LN(1.05))::int)
  END;
$$;

GRANT EXECUTE ON FUNCTION public._level_from_xp(integer) TO authenticated, anon, service_role;

-- Function pure : Niveau N → XP cumulée nécessaire pour ATTEINDRE ce niveau.
-- Utilisée par le front pour calculer xpToNextLevel.
CREATE OR REPLACE FUNCTION public._xp_for_level(p_level integer)
RETURNS integer LANGUAGE sql IMMUTABLE AS $$
  SELECT CASE
    WHEN p_level <= 1 THEN 0
    WHEN p_level = 2  THEN 5
    WHEN p_level = 3  THEN 13
    WHEN p_level >= 50 THEN 4467  -- cap atteint à ~4467 XP (cumul niveau 50)
    ELSE (13 + 500 * (POWER(1.05, p_level - 3) - 1))::int
  END;
$$;

GRANT EXECUTE ON FUNCTION public._xp_for_level(integer) TO authenticated, anon, service_role;
```

- [ ] **Step 2: Appliquer la migration via Supabase CLI**

```bash
cd "C:/Users/uriel/Desktop/DEVS/app (Runes de Chêne)"
pnpm dlx supabase db push
```

Expected: `Applying migration 040_v07_levels_schema.sql... Done.`

- [ ] **Step 3: Vérifier la function en SQL**

Dans Supabase Studio SQL Editor, exécuter :

```sql
SELECT
  public._level_from_xp(0)    AS lvl_at_0,    -- attendu : 1
  public._level_from_xp(4)    AS lvl_at_4,    -- attendu : 1
  public._level_from_xp(5)    AS lvl_at_5,    -- attendu : 2
  public._level_from_xp(12)   AS lvl_at_12,   -- attendu : 2
  public._level_from_xp(13)   AS lvl_at_13,   -- attendu : 3
  public._level_from_xp(37)   AS lvl_at_37,   -- attendu : 3
  public._level_from_xp(38)   AS lvl_at_38,   -- attendu : 4
  public._level_from_xp(92)   AS lvl_at_92,   -- attendu : 6
  public._level_from_xp(1379) AS lvl_at_1379, -- attendu : 30
  public._level_from_xp(4467) AS lvl_at_4467, -- attendu : 50
  public._level_from_xp(99999) AS lvl_at_max; -- attendu : 50 (cap)
```

Tous les résultats doivent matcher les valeurs attendues. Si non, revoir la formule SQL.

- [ ] **Step 4: Vérifier `_xp_for_level`**

```sql
SELECT
  public._xp_for_level(1)  AS xp_lvl_1,  -- 0
  public._xp_for_level(2)  AS xp_lvl_2,  -- 5
  public._xp_for_level(3)  AS xp_lvl_3,  -- 13
  public._xp_for_level(4)  AS xp_lvl_4,  -- 38
  public._xp_for_level(30) AS xp_lvl_30, -- ~1379
  public._xp_for_level(50) AS xp_lvl_50; -- 4467
```

- [ ] **Step 5: Vérifier xp_epoch enregistré**

```sql
SELECT key, value FROM public.app_config WHERE key = 'xp_epoch';
```

Doit retourner une ligne avec un timestamp epoch récent (ex `1714578123`).

- [ ] **Step 6: Vérifier les colonnes ajoutées**

```sql
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_schema='public' AND table_name='users'
  AND column_name IN ('xp_total', 'veteran_first_era');
```

Doit retourner 2 lignes : xp_total integer DEFAULT 0, veteran_first_era boolean DEFAULT false.

- [ ] **Step 7: Commit**

```bash
git add supabase/migrations/040_v07_levels_schema.sql
git commit -m "feat(v0.7): mig 040 — schéma niveaux (xp_total, veteran_first_era, _level_from_xp)"
git push
```

---

## Phase 2 — Triggers d'incrémentation/décrémentation

### Task 2: Migration triggers `041_v07_levels_triggers.sql`

**Files:**
- Create: `supabase/migrations/041_v07_levels_triggers.sql`

- [ ] **Step 1: Créer le fichier migration**

```sql
-- 041_v07_levels_triggers.sql
-- WHY : Phase 2 du système Niveaux V0.7.
-- Triggers INSERT/DELETE sur les 5 tables d'action qui incrémentent/décrémentent
-- xp_total des users concernés. Le delta est conditionné par la date :
--   created_at >= xp_epoch → trigger actif
--   created_at <  xp_epoch → no-op (protège l'historique pré-switch)
--
-- Coefficients (cf. spec §5) :
--   places_discovered : +1 / -1
--   place_explorers   : +3 / -3
--   places            : +20 / -20
--   place_contributions type='carnet' : +5 / -5
--   place_contributions type='photo'  : +1 par photo dans images[] / -N
--   veille_history    : +10 / jamais (DELETE no-op — événement immuable)
--   enigma_responses (correct=true) : +1/+1/+2/+3 selon difficulté / jamais

-- Helper interne pour lire xp_epoch (cast string → bigint timestamp epoch)
CREATE OR REPLACE FUNCTION public._xp_epoch()
RETURNS timestamptz LANGUAGE sql STABLE AS $$
  SELECT to_timestamp((SELECT value::bigint FROM public.app_config WHERE key='xp_epoch'));
$$;

-- ============================================================
-- TRIGGER : places_discovered (Découvrir, +1)
-- ============================================================
CREATE OR REPLACE FUNCTION public._trg_xp_discovered_insert()
RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
  IF NEW.created_at >= public._xp_epoch() THEN
    UPDATE public.users SET xp_total = xp_total + 1 WHERE id = NEW.user_id;
  END IF;
  RETURN NEW;
END;
$$;

CREATE OR REPLACE FUNCTION public._trg_xp_discovered_delete()
RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
  IF OLD.created_at >= public._xp_epoch() THEN
    UPDATE public.users SET xp_total = GREATEST(0, xp_total - 1) WHERE id = OLD.user_id;
  END IF;
  RETURN OLD;
END;
$$;

DROP TRIGGER IF EXISTS trg_xp_discovered_ins ON public.places_discovered;
DROP TRIGGER IF EXISTS trg_xp_discovered_del ON public.places_discovered;
CREATE TRIGGER trg_xp_discovered_ins AFTER INSERT ON public.places_discovered
  FOR EACH ROW EXECUTE FUNCTION public._trg_xp_discovered_insert();
CREATE TRIGGER trg_xp_discovered_del AFTER DELETE ON public.places_discovered
  FOR EACH ROW EXECUTE FUNCTION public._trg_xp_discovered_delete();

-- ============================================================
-- TRIGGER : place_explorers (Fouler, +3)
-- ============================================================
CREATE OR REPLACE FUNCTION public._trg_xp_explorer_insert()
RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
  -- DISTINCT garanti par PRIMARY KEY (user_id, place_id) sur place_explorers
  IF COALESCE(NEW.visited_at, now()) >= public._xp_epoch() THEN
    UPDATE public.users SET xp_total = xp_total + 3 WHERE id = NEW.user_id;
  END IF;
  RETURN NEW;
END;
$$;

CREATE OR REPLACE FUNCTION public._trg_xp_explorer_delete()
RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
  IF COALESCE(OLD.visited_at, now()) >= public._xp_epoch() THEN
    UPDATE public.users SET xp_total = GREATEST(0, xp_total - 3) WHERE id = OLD.user_id;
  END IF;
  RETURN OLD;
END;
$$;

DROP TRIGGER IF EXISTS trg_xp_explorer_ins ON public.place_explorers;
DROP TRIGGER IF EXISTS trg_xp_explorer_del ON public.place_explorers;
CREATE TRIGGER trg_xp_explorer_ins AFTER INSERT ON public.place_explorers
  FOR EACH ROW EXECUTE FUNCTION public._trg_xp_explorer_insert();
CREATE TRIGGER trg_xp_explorer_del AFTER DELETE ON public.place_explorers
  FOR EACH ROW EXECUTE FUNCTION public._trg_xp_explorer_delete();

-- ============================================================
-- TRIGGER : places (Cartographier, +20)
-- ============================================================
CREATE OR REPLACE FUNCTION public._trg_xp_place_insert()
RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
  IF NEW.author_id IS NOT NULL AND NEW.created_at >= public._xp_epoch() THEN
    UPDATE public.users SET xp_total = xp_total + 20 WHERE id = NEW.author_id;
  END IF;
  RETURN NEW;
END;
$$;

CREATE OR REPLACE FUNCTION public._trg_xp_place_delete()
RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
  IF OLD.author_id IS NOT NULL AND OLD.created_at >= public._xp_epoch() THEN
    UPDATE public.users SET xp_total = GREATEST(0, xp_total - 20) WHERE id = OLD.author_id;
  END IF;
  RETURN OLD;
END;
$$;

DROP TRIGGER IF EXISTS trg_xp_place_ins ON public.places;
DROP TRIGGER IF EXISTS trg_xp_place_del ON public.places;
CREATE TRIGGER trg_xp_place_ins AFTER INSERT ON public.places
  FOR EACH ROW EXECUTE FUNCTION public._trg_xp_place_insert();
CREATE TRIGGER trg_xp_place_del AFTER DELETE ON public.places
  FOR EACH ROW EXECUTE FUNCTION public._trg_xp_place_delete();

-- ============================================================
-- TRIGGER : place_contributions (Carnet +5, Photo +1 par photo)
-- ============================================================
CREATE OR REPLACE FUNCTION public._trg_xp_contribution_insert()
RETURNS trigger LANGUAGE plpgsql AS $$
DECLARE
  v_delta integer := 0;
BEGIN
  IF NEW.created_at < public._xp_epoch() THEN RETURN NEW; END IF;
  IF NEW.type = 'carnet' THEN
    v_delta := 5;
  ELSIF NEW.type = 'photo' THEN
    -- Compte le nombre de photos dans images[] (logique mig 038)
    v_delta := COALESCE(jsonb_array_length(NEW.images), 0)
             + CASE WHEN (NEW.images IS NULL OR jsonb_array_length(NEW.images) = 0)
                     AND NEW.image_url IS NOT NULL AND NEW.image_url != ''
                    THEN 1 ELSE 0 END;
  END IF;
  IF v_delta > 0 THEN
    UPDATE public.users SET xp_total = xp_total + v_delta WHERE id = NEW.user_id;
  END IF;
  RETURN NEW;
END;
$$;

CREATE OR REPLACE FUNCTION public._trg_xp_contribution_delete()
RETURNS trigger LANGUAGE plpgsql AS $$
DECLARE
  v_delta integer := 0;
BEGIN
  IF OLD.created_at < public._xp_epoch() THEN RETURN OLD; END IF;
  IF OLD.type = 'carnet' THEN
    v_delta := 5;
  ELSIF OLD.type = 'photo' THEN
    v_delta := COALESCE(jsonb_array_length(OLD.images), 0)
             + CASE WHEN (OLD.images IS NULL OR jsonb_array_length(OLD.images) = 0)
                     AND OLD.image_url IS NOT NULL AND OLD.image_url != ''
                    THEN 1 ELSE 0 END;
  END IF;
  IF v_delta > 0 THEN
    UPDATE public.users SET xp_total = GREATEST(0, xp_total - v_delta) WHERE id = OLD.user_id;
  END IF;
  RETURN OLD;
END;
$$;

DROP TRIGGER IF EXISTS trg_xp_contribution_ins ON public.place_contributions;
DROP TRIGGER IF EXISTS trg_xp_contribution_del ON public.place_contributions;
CREATE TRIGGER trg_xp_contribution_ins AFTER INSERT ON public.place_contributions
  FOR EACH ROW EXECUTE FUNCTION public._trg_xp_contribution_insert();
CREATE TRIGGER trg_xp_contribution_del AFTER DELETE ON public.place_contributions
  FOR EACH ROW EXECUTE FUNCTION public._trg_xp_contribution_delete();

-- ============================================================
-- TRIGGER : veille_history (Plantage +10, INSERT only — événement immuable)
-- ============================================================
CREATE OR REPLACE FUNCTION public._trg_xp_plantage_insert()
RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
  IF COALESCE(NEW.planted_at, now()) >= public._xp_epoch() THEN
    UPDATE public.users SET xp_total = xp_total + 10 WHERE id = NEW.user_id;
  END IF;
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_xp_plantage_ins ON public.veille_history;
CREATE TRIGGER trg_xp_plantage_ins AFTER INSERT ON public.veille_history
  FOR EACH ROW EXECUTE FUNCTION public._trg_xp_plantage_insert();

-- ============================================================
-- TRIGGER : enigma_responses (Énigme +1/+1/+2/+3, INSERT only — immuable)
-- ============================================================
CREATE OR REPLACE FUNCTION public._trg_xp_enigma_insert()
RETURNS trigger LANGUAGE plpgsql AS $$
DECLARE
  v_delta integer := 0;
  v_difficulty text;
BEGIN
  IF NOT NEW.correct THEN RETURN NEW; END IF;
  IF COALESCE(NEW.responded_at, now()) < public._xp_epoch() THEN RETURN NEW; END IF;
  SELECT difficulty INTO v_difficulty FROM public.enigmas WHERE id = NEW.enigma_id;
  v_delta := CASE v_difficulty
    WHEN 'very_easy' THEN 1
    WHEN 'easy'      THEN 1
    WHEN 'medium'    THEN 2
    WHEN 'hard'      THEN 3
    ELSE 1
  END;
  UPDATE public.users SET xp_total = xp_total + v_delta WHERE id = NEW.user_id;
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_xp_enigma_ins ON public.enigma_responses;
CREATE TRIGGER trg_xp_enigma_ins AFTER INSERT ON public.enigma_responses
  FOR EACH ROW EXECUTE FUNCTION public._trg_xp_enigma_insert();
```

- [ ] **Step 2: Appliquer la migration**

```bash
cd "C:/Users/uriel/Desktop/DEVS/app (Runes de Chêne)"
pnpm dlx supabase db push
```

- [ ] **Step 3: Test trigger places_discovered (devrait incrémenter +1)**

Dans Supabase Studio, choisir un user_id de test et un place_id existant non encore découvert :

```sql
-- Note xp_total avant
SELECT id, xp_total FROM public.users WHERE id = '<USER_ID_TEST>';

-- Insère une découverte (simule discover_place remote)
INSERT INTO public.places_discovered (user_id, place_id, method)
VALUES ('<USER_ID_TEST>', '<PLACE_ID>', 'remote');

-- Vérifie que xp_total a augmenté de +1
SELECT id, xp_total FROM public.users WHERE id = '<USER_ID_TEST>';
```

Expected : xp_total a augmenté exactement de +1.

- [ ] **Step 4: Test trigger DELETE places_discovered (devrait décrémenter -1)**

```sql
DELETE FROM public.places_discovered
WHERE user_id = '<USER_ID_TEST>' AND place_id = '<PLACE_ID>';

SELECT id, xp_total FROM public.users WHERE id = '<USER_ID_TEST>';
```

Expected : xp_total revenu à sa valeur initiale.

- [ ] **Step 5: Test trigger garde xp_epoch (INSERT avec created_at < epoch ne touche rien)**

```sql
-- Insère une découverte avec created_at FORCÉ dans le passé (pré-epoch)
INSERT INTO public.places_discovered (user_id, place_id, method, created_at)
VALUES ('<USER_ID_TEST>', '<PLACE_ID>', 'remote', '2020-01-01'::timestamptz);

-- Vérifie que xp_total N'A PAS bougé
SELECT id, xp_total FROM public.users WHERE id = '<USER_ID_TEST>';

-- Cleanup
DELETE FROM public.places_discovered WHERE user_id = '<USER_ID_TEST>' AND place_id = '<PLACE_ID>';
```

Expected : xp_total reste identique (pré-epoch ignoré).

- [ ] **Step 6: Tests rapides sur les autres triggers**

Répéter le pattern Step 3 pour :
- `place_explorers` INSERT → +3
- `place_contributions` INSERT type='carnet' → +5
- `veille_history` INSERT → +10
(Si tu n'as pas le temps de tester chacun manuellement, au moins lancer un cycle dev puis vérifier que le compteur xp_total bouge cohéremment quand tu utilises l'app.)

- [ ] **Step 7: Commit**

```bash
git add supabase/migrations/041_v07_levels_triggers.sql
git commit -m "feat(v0.7): mig 041 — triggers XP sur 5 tables d'action (cap par xp_epoch)"
git push
```

---

## Phase 3 — Refonte des titres généraux

### Task 3: Migration titres `042_v07_levels_titles_refonte.sql`

**Files:**
- Create: `supabase/migrations/042_v07_levels_titles_refonte.sql`

- [ ] **Step 1: Créer le fichier migration (DELETE + UPDATE + INSERT)**

```sql
-- 042_v07_levels_titles_refonte.sql
-- WHY : Refonte complète des titres généraux V0.7 (cf. spec §8).
-- 33 titres finaux sur 7 axes thématiques, tous threshold acquis à vie.
-- Plus aucun rank lifetime (qui frustrait en se faisant doubler).
--
-- Conditions JSONB normalisées sur les nouveaux compteurs :
--   level, discoveries, places_visited, enigma_score, plantages, places_added, carnets

-- ============================================================
-- 1. DELETE des titres dépréciés
-- ============================================================
DELETE FROM public.titles WHERE id IN (18, 19); -- Bâtisseur, Bâtisseur de cathédrales (concept fortifications disparu)

-- ============================================================
-- 2. UPDATE des titres existants vers les nouvelles conditions
-- ============================================================

-- Axe 1 — Niveau (refonte des anciens rank-glory)
UPDATE public.titles SET name='Légende',         icon='👑', "order"=8,  condition='{"stat":"level","min":50}'::jsonb, description='Tu as atteint le sommet — niveau 50.' WHERE id=20;
UPDATE public.titles SET name='Héros régional',  icon='🏛️', "order"=21, condition='{"stat":"level","min":35}'::jsonb, description='Niveau 35 atteint.' WHERE id=24;
UPDATE public.titles SET name='Héros local',     icon='🛡️', "order"=20, condition='{"stat":"level","min":25}'::jsonb, description='Niveau 25 atteint.' WHERE id=23;

-- Axe 2 — Découvertes brouillard (existants, conservés mais seuils ajustés)
UPDATE public.titles SET name='Novice',     icon='🌱', condition='{"stat":"discoveries","min":0}'::jsonb,   description='Tu viens de t''inscrire... c''est déjà bien.' WHERE id=1;
UPDATE public.titles SET name='Explorateur',icon='🧭', condition='{"stat":"discoveries","min":50}'::jsonb,  description='Tu as découvert plus de 50 lieux.' WHERE id=2;
UPDATE public.titles SET name='Arpenteur',  icon='🗺️', condition='{"stat":"discoveries","min":200}'::jsonb, description='Tu as découvert plus de 200 lieux.' WHERE id=3;

-- Axe 3 — Marche physique (refonte des anciens "exploration" morts)
UPDATE public.titles SET name='Pèlerin',              icon='🥾', "order"=30, condition='{"stat":"places_visited","min":10}'::jsonb,  description='Tu as foulé 10 lieux.' WHERE id=25;
UPDATE public.titles SET name='Cheminant',            icon='🚶', "order"=31, condition='{"stat":"places_visited","min":50}'::jsonb,  description='Tu as foulé 50 lieux.' WHERE id=26;
UPDATE public.titles SET name='Marcheur des Mondes',  icon='🌍', "order"=33, condition='{"stat":"places_visited","min":500}'::jsonb, description='Tu as foulé 500 lieux.' WHERE id=27;

-- Axe 4 — Érudition (refonte des anciens "erudition" morts)
UPDATE public.titles SET name='Érudit',     icon='📚', "order"=40, condition='{"stat":"enigma_score","min":50}'::jsonb,  description='50 points d''énigmes pondérés.' WHERE id=28;
UPDATE public.titles SET name='Philosophe', icon='🦉', "order"=41, condition='{"stat":"enigma_score","min":150}'::jsonb, description='150 points d''énigmes pondérés.' WHERE id=29;
UPDATE public.titles SET name='Grand Sage', icon='📖', "order"=42, condition='{"stat":"enigma_score","min":400}'::jsonb, description='400 points d''énigmes pondérés.' WHERE id=30;

-- Axe 5 — Bannière (refonte des anciens claims morts)
UPDATE public.titles SET name='Hérault',            icon='🏴', "order"=51, condition='{"stat":"plantages","min":10}'::jsonb,  description='Tu as planté 10 bannières.' WHERE id=5;
UPDATE public.titles SET name='Maréchal',           icon='👑', "order"=54, condition='{"stat":"plantages","min":200}'::jsonb, description='Tu as planté 200 bannières.' WHERE id=17;

-- Axe 6 — Cartographie (refonte des anciens rank places_added)
UPDATE public.titles SET name='Cartographe Initié', icon='🧭', "order"=61, condition='{"stat":"places_added","min":5}'::jsonb,   description='Tu as cartographié 5 lieux.' WHERE id=31;
UPDATE public.titles SET name='Cartographe',        icon='📐', "order"=62, condition='{"stat":"places_added","min":25}'::jsonb,  description='Tu as cartographié 25 lieux.' WHERE id=32;
UPDATE public.titles SET name='Maître-Cartographe', icon='🏗️', "order"=64, condition='{"stat":"places_added","min":500}'::jsonb, description='Tu as cartographié 500 lieux.' WHERE id=33;
-- ID 22 (Grand Chroniqueur) ajusté en place 100
UPDATE public.titles SET name='Grand Chroniqueur',  icon='📜', "order"=63, condition='{"stat":"places_added","min":100}'::jsonb, description='Tu as cartographié 100 lieux.' WHERE id=22;

-- ============================================================
-- 3. INSERT des nouveaux titres pour compléter à 33
-- ============================================================

-- Axe 1 — Niveau, paliers manquants (Compagnon, Veilleur, Héros)
INSERT INTO public.titles (name, type, faction_id, "order", icon, unlocks, condition, description) VALUES
  ('Compagnon', 'general', NULL, 4,  '⚜️', '{}'::text[], '{"stat":"level","min":5}'::jsonb,  'Niveau 5 atteint.'),
  ('Veilleur',  'general', NULL, 5,  '⚔️', '{}'::text[], '{"stat":"level","min":15}'::jsonb, 'Niveau 15 atteint. Tu portes le titre de Veilleur.'),
  ('Héros',     'general', NULL, 22, '🦅', '{}'::text[], '{"stat":"level","min":42}'::jsonb, 'Niveau 42 atteint.');

-- Axe 2 — Découvertes brouillard, paliers manquants (Curieux, Grand Voyageur)
INSERT INTO public.titles (name, type, faction_id, "order", icon, unlocks, condition, description) VALUES
  ('Curieux',        'general', NULL, 1,  '🔍', '{}'::text[], '{"stat":"discoveries","min":10}'::jsonb,   'Tu as découvert plus de 10 lieux.'),
  ('Grand Voyageur', 'general', NULL, 4,  '🌐', '{}'::text[], '{"stat":"discoveries","min":1000}'::jsonb, 'Tu as découvert plus de 1000 lieux.');

-- Axe 3 — Marche, palier manquant (Errant, ID 26 était Arpenteur doublon → renommé Cheminant ci-dessus, donc on ajoute Errant)
INSERT INTO public.titles (name, type, faction_id, "order", icon, unlocks, condition, description) VALUES
  ('Errant', 'general', NULL, 32, '⚔️', '{}'::text[], '{"stat":"places_visited","min":150}'::jsonb, 'Tu as foulé 150 lieux. Tel un chevalier errant.');

-- Axe 4 — Érudition, palier manquant (Apprenti Sage)
INSERT INTO public.titles (name, type, faction_id, "order", icon, unlocks, condition, description) VALUES
  ('Apprenti Sage', 'general', NULL, 39, '📜', '{}'::text[], '{"stat":"enigma_score","min":15}'::jsonb, '15 points d''énigmes pondérés.');

-- Axe 5 — Bannière, paliers manquants (Recrue, Banneret, Capitaine)
INSERT INTO public.titles (name, type, faction_id, "order", icon, unlocks, condition, description) VALUES
  ('Recrue',    'general', NULL, 50, '🌑', '{}'::text[], '{"stat":"plantages","min":3}'::jsonb,  'Tu as planté 3 bannières.'),
  ('Banneret',  'general', NULL, 52, '⚜️', '{}'::text[], '{"stat":"plantages","min":30}'::jsonb, 'Tu as planté 30 bannières.'),
  ('Capitaine', 'general', NULL, 53, '🛡️', '{}'::text[], '{"stat":"plantages","min":80}'::jsonb, 'Tu as planté 80 bannières.');

-- Axe 6 — Cartographie, palier manquant (Pionnier)
INSERT INTO public.titles (name, type, faction_id, "order", icon, unlocks, condition, description) VALUES
  ('Pionnier', 'general', NULL, 60, '🌟', '{}'::text[], '{"stat":"places_added","min":1}'::jsonb, 'Tu as cartographié ton premier lieu.');

-- Axe 7 — Carnets (NOUVEAU axe complet, 4 titres)
INSERT INTO public.titles (name, type, faction_id, "order", icon, unlocks, condition, description) VALUES
  ('Page',           'general', NULL, 70, '📝', '{}'::text[], '{"stat":"carnets","min":1}'::jsonb,   'Ton premier récit.'),
  ('Conteur',        'general', NULL, 71, '🪶', '{}'::text[], '{"stat":"carnets","min":10}'::jsonb,  'Tu as écrit 10 récits.'),
  ('Chroniqueur',    'general', NULL, 72, '📜', '{}'::text[], '{"stat":"carnets","min":50}'::jsonb,  'Tu as écrit 50 récits.'),
  ('Maître Conteur', 'general', NULL, 73, '📖', '{}'::text[], '{"stat":"carnets","min":200}'::jsonb, 'Tu as écrit 200 récits.');
```

- [ ] **Step 2: Appliquer la migration**

```bash
pnpm dlx supabase db push
```

- [ ] **Step 3: Vérifier les 33 titres généraux**

```sql
SELECT id, name, "order", condition
FROM public.titles
WHERE type = 'general'
ORDER BY "order";
```

Expected : 33 lignes, conditions JSONB toutes valides.

- [ ] **Step 4: Vérifier qu'il n'y a aucune condition héritée morte**

```sql
SELECT id, name, condition
FROM public.titles
WHERE type='general'
  AND (condition->>'stat' IN ('glory', 'erudition', 'exploration', 'fortifications', 'claims', 'notoriety')
       OR condition ? 'rank');
```

Expected : 0 lignes. Si non, c'est qu'un UPDATE a raté — debug.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/042_v07_levels_titles_refonte.sql
git commit -m "feat(v0.7): mig 042 — refonte titres généraux (33 titres, 7 axes, tous threshold)"
git push
```

---

### Task 4: Migration `043_v07_levels_get_user_titles.sql` — refonte de la fonction d'évaluation

**Files:**
- Create: `supabase/migrations/043_v07_levels_get_user_titles.sql`

- [ ] **Step 1: Créer le fichier migration**

```sql
-- 043_v07_levels_get_user_titles.sql
-- WHY : refonte de get_user_titles pour évaluer les titres généraux sur les
-- nouveaux compteurs (level, places_visited, enigma_score, plantages, carnets).
-- Reprise EXACTE de mig 038 + ajout des nouveaux CASE + suppression des stats mortes.

CREATE OR REPLACE FUNCTION public.get_user_titles(p_user_id text)
RETURNS json
LANGUAGE plpgsql SECURITY DEFINER
AS $$
DECLARE
  v_faction_id TEXT;
  v_xp_total INT;
  v_level INT;
  v_displayed_ids INT[];
  v_discoveries INT;
  v_places_visited INT;
  v_places_added INT;
  v_carnets INT;
  v_plantages INT;
  v_enigma_score INT;
  v_general JSON;
  v_faction2 JSON;
  v_general_arr JSON[] := '{}';
  v_player_rank INT;
  v_player_coupe_score INT;
  v_season_start timestamptz;
  v_season_end   timestamptz;
BEGIN
  -- Compteurs joueur (V0.7)
  SELECT COUNT(*) INTO v_discoveries FROM places_discovered WHERE user_id = p_user_id;
  SELECT COUNT(DISTINCT place_id) INTO v_places_visited FROM public.place_explorers WHERE user_id = p_user_id;
  SELECT COUNT(*) INTO v_places_added FROM places WHERE author_id = p_user_id;
  SELECT COUNT(*) INTO v_carnets FROM public.place_contributions WHERE user_id = p_user_id AND type = 'carnet';
  SELECT COUNT(*) INTO v_plantages FROM public.veille_history WHERE user_id = p_user_id;
  v_enigma_score := public._enigma_score_weighted(p_user_id);

  -- xp_total + niveau dérivé
  SELECT COALESCE(xp_total, 0), faction_id, COALESCE(displayed_general_title_ids, '{}')
    INTO v_xp_total, v_faction_id, v_displayed_ids
    FROM users WHERE id = p_user_id;
  v_level := public._level_from_xp(v_xp_total);

  -- Évaluation des titres généraux (threshold sur les compteurs étendus)
  FOR v_general IN
    SELECT json_build_object(
      'id', t.id, 'name', t.name, 'icon', t.icon, 'unlocks', t.unlocks, 'order', t."order", 'type', 'general',
      'unlocked', CASE
        WHEN t.condition IS NULL THEN false
        WHEN t.condition->>'stat' = 'level'           THEN v_level >= (t.condition->>'min')::INT
        WHEN t.condition->>'stat' = 'discoveries'     THEN v_discoveries >= (t.condition->>'min')::INT
        WHEN t.condition->>'stat' = 'places_visited'  THEN v_places_visited >= (t.condition->>'min')::INT
        WHEN t.condition->>'stat' = 'enigma_score'    THEN v_enigma_score >= (t.condition->>'min')::INT
        WHEN t.condition->>'stat' = 'plantages'       THEN v_plantages >= (t.condition->>'min')::INT
        WHEN t.condition->>'stat' = 'places_added'    THEN v_places_added >= (t.condition->>'min')::INT
        WHEN t.condition->>'stat' = 'carnets'         THEN v_carnets >= (t.condition->>'min')::INT
        ELSE false
      END
    )
    FROM titles t WHERE t.type = 'general' ORDER BY t."order"
  LOOP
    v_general_arr := array_append(v_general_arr, v_general);
  END LOOP;

  -- Titres faction (inchangés vs mig 038 — toujours basés sur Coupe saison)
  v_faction2 := NULL;
  IF v_faction_id IS NOT NULL THEN
    SELECT started_at, COALESCE(ended_at, now())
    INTO v_season_start, v_season_end
    FROM public.coupe_seasons
    ORDER BY (ended_at IS NULL) DESC, started_at DESC
    LIMIT 1;
    IF v_season_start IS NULL THEN v_season_start := 'epoch'::timestamptz; v_season_end := now(); END IF;

    WITH faction_users AS (SELECT id FROM users WHERE faction_id = v_faction_id),
    s_visites   AS (SELECT user_id, COUNT(DISTINCT place_id)::int AS cnt FROM public.place_explorers WHERE user_id IN (SELECT id FROM faction_users) AND visited_at >= v_season_start AND visited_at < v_season_end GROUP BY user_id),
    s_places    AS (SELECT author_id AS user_id, COUNT(*)::int AS cnt FROM public.places WHERE author_id IN (SELECT id FROM faction_users) AND created_at >= v_season_start AND created_at < v_season_end GROUP BY author_id),
    s_carnets   AS (SELECT user_id, COUNT(*)::int AS cnt FROM public.place_contributions WHERE user_id IN (SELECT id FROM faction_users) AND type = 'carnet' AND created_at >= v_season_start AND created_at < v_season_end GROUP BY user_id),
    s_photos    AS (
      SELECT user_id, COALESCE(SUM(
        COALESCE(jsonb_array_length(images), 0)
        + CASE WHEN (images IS NULL OR jsonb_array_length(images) = 0) AND image_url IS NOT NULL AND image_url != '' THEN 1 ELSE 0 END
      ), 0)::int AS cnt
      FROM public.place_contributions WHERE user_id IN (SELECT id FROM faction_users) AND created_at >= v_season_start AND created_at < v_season_end GROUP BY user_id
    ),
    s_plantages AS (SELECT user_id, COUNT(*)::int AS cnt FROM public.veille_history WHERE user_id IN (SELECT id FROM faction_users) AND planted_at >= v_season_start AND planted_at < v_season_end GROUP BY user_id),
    s_enigmes   AS (SELECT fu.id AS user_id, public._enigma_score_weighted(fu.id, v_season_start, v_season_end) AS cnt FROM faction_users fu),
    user_coupe AS (
      SELECT fu.id, (COALESCE(sv.cnt,0)*1 + COALESCE(sp.cnt,0)*7 + COALESCE(sc.cnt,0)*3 + COALESCE(sph.cnt,0)*1 + COALESCE(spl.cnt,0)*5 + COALESCE(se.cnt,0))::int AS coupe_score
      FROM faction_users fu
      LEFT JOIN s_visites sv ON sv.user_id = fu.id
      LEFT JOIN s_places sp ON sp.user_id = fu.id
      LEFT JOIN s_carnets sc ON sc.user_id = fu.id
      LEFT JOIN s_photos sph ON sph.user_id = fu.id
      LEFT JOIN s_plantages spl ON spl.user_id = fu.id
      LEFT JOIN s_enigmes se ON se.user_id = fu.id
    ),
    ranked AS (SELECT id, coupe_score, ROW_NUMBER() OVER (ORDER BY coupe_score DESC, id)::int AS rk FROM user_coupe)
    SELECT rk, coupe_score INTO v_player_rank, v_player_coupe_score FROM ranked WHERE id = p_user_id;

    IF v_player_coupe_score IS NULL OR v_player_coupe_score <= 0 THEN
      v_faction2 := NULL;
    ELSE
      SELECT json_build_object('id', t.id, 'name', t.name, 'icon', t.icon, 'unlocks', t.unlocks, 'type', 'faction')
      INTO v_faction2
      FROM titles t
      WHERE t.type = 'faction' AND t.faction_id = v_faction_id
        AND t.condition IS NOT NULL AND (t.condition->>'rank') IS NOT NULL
        AND v_player_rank <= (t.condition->>'rank')::INT
      ORDER BY (t.condition->>'rank')::INT ASC
      LIMIT 1;
    END IF;
  END IF;

  RETURN json_build_object(
    'unlockedGeneralTitles', COALESCE((SELECT json_agg(elem) FROM unnest(v_general_arr) AS elem WHERE (elem->>'unlocked')::boolean = true), '[]'::json),
    'displayedGeneralTitleIds', v_displayed_ids,
    'factionTitle', v_faction2,
    'stats', json_build_object(
      'level', v_level,
      'xpTotal', v_xp_total,
      'discoveries', v_discoveries,
      'places_visited', v_places_visited,
      'places_added', v_places_added,
      'carnets', v_carnets,
      'plantages', v_plantages,
      'enigma_score', v_enigma_score
    )
  );
END;
$$;

GRANT EXECUTE ON FUNCTION public.get_user_titles(text) TO authenticated, anon, service_role;
```

- [ ] **Step 2: Appliquer la migration**

```bash
pnpm dlx supabase db push
```

- [ ] **Step 3: Test sur un user**

```sql
SELECT public.get_user_titles('<USER_ID_TEST>');
```

Vérifier dans le JSON retourné :
- `stats.level` = 1 (puisque xp_total = 0 pour tous au démarrage)
- `stats.xpTotal` = 0
- `unlockedGeneralTitles` contient au moins "Novice" (min discoveries = 0)
- Aucune erreur SQL

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/043_v07_levels_get_user_titles.sql
git commit -m "feat(v0.7): mig 043 — refonte get_user_titles (évaluation sur compteurs étendus)"
git push
```

---

## Phase 4 — Refonte des RPCs principales

### Task 5: Migration `044_v07_levels_player_profile.sql` — refonte get_player_profile

**Files:**
- Create: `supabase/migrations/044_v07_levels_player_profile.sql`

- [ ] **Step 1: Créer le fichier**

```sql
-- 044_v07_levels_player_profile.sql
-- WHY : refonte de get_player_profile pour exposer level/xpTotal/xpToNextLevel/
-- veteranFirstEra et retirer glory/explorationPoints/eruditionPoints.
-- Reprise mig 038 avec adaptations.

CREATE OR REPLACE FUNCTION public.get_player_profile(p_user_id text)
RETURNS json
LANGUAGE plpgsql SECURITY DEFINER
AS $$
DECLARE
  v_result JSON;
  v_titles_data JSON;
  v_displayed_v3 INT[];
  v_displayed_general JSON;
  v_faction_title JSON;
  v_authored_places JSON;
  v_discovered_places JSON;
  v_favorite_places JSON;
  v_veilled_places JSON;
  v_unlocked_ids INT[];
  v_faction_title_id INT;
  v_xp_total INT;
  v_level INT;
  v_xp_to_next INT;
  v_xp_for_next_level INT;
  v_lieux_explores INT;
  v_lieux_veilles INT;
  v_enigmas_solved INT;
BEGIN
  v_titles_data := get_user_titles(p_user_id);
  v_faction_title := v_titles_data->'factionTitle';

  IF v_faction_title IS NOT NULL THEN
    v_faction_title_id := (v_faction_title->>'id')::INT;
  END IF;

  SELECT array_agg((elem->>'id')::INT) INTO v_unlocked_ids
  FROM json_array_elements(v_titles_data->'unlockedGeneralTitles') elem;
  v_unlocked_ids := COALESCE(v_unlocked_ids, '{}');

  IF v_faction_title_id IS NOT NULL THEN
    v_unlocked_ids := v_unlocked_ids || v_faction_title_id;
  END IF;

  SELECT COALESCE(displayed_title_ids_v3, '{}'), COALESCE(xp_total, 0)
    INTO v_displayed_v3, v_xp_total
    FROM users WHERE id = p_user_id;
  v_level := public._level_from_xp(v_xp_total);
  v_xp_for_next_level := public._xp_for_level(v_level + 1);
  v_xp_to_next := GREATEST(0, v_xp_for_next_level - v_xp_total);

  -- Filtrage des titres affichés (idem mig 038)
  IF array_length(v_displayed_v3, 1) > 0 THEN
    SELECT json_agg(row_data ORDER BY pos) INTO v_displayed_general
    FROM (
      SELECT t.id, t.name, t.icon, NULL::TEXT AS icon_url, array_position(v_displayed_v3, t.id) AS pos
      FROM titles t WHERE t.id = ANY(v_displayed_v3) AND t.id > 0 AND t.id = ANY(v_unlocked_ids)
      UNION ALL
      SELECT fw.id * -1 AS id, fw.word AS name, tf.icon, tf.icon_url, array_position(v_displayed_v3, fw.id * -1) AS pos
      FROM fragment_words fw JOIN title_fragments tf ON tf.id = fw.fragment_id
      WHERE (fw.id * -1) = ANY(v_displayed_v3)
        AND EXISTS (SELECT 1 FROM user_fragments uf WHERE uf.user_id = p_user_id AND uf.fragment_id = tf.id)
    ) row_data;

    UPDATE users
    SET displayed_title_ids_v3 = (
      SELECT COALESCE(array_agg(tid), '{}')
      FROM unnest(v_displayed_v3) AS tid
      WHERE tid = ANY(v_unlocked_ids)
        OR (tid < 0 AND EXISTS (
          SELECT 1 FROM user_fragments uf
          JOIN fragment_words fw ON fw.fragment_id = uf.fragment_id
          WHERE uf.user_id = p_user_id AND fw.id * -1 = tid
        ))
    )
    WHERE id = p_user_id
      AND displayed_title_ids_v3 IS DISTINCT FROM (
        SELECT COALESCE(array_agg(tid), '{}')
        FROM unnest(v_displayed_v3) AS tid
        WHERE tid = ANY(v_unlocked_ids)
          OR (tid < 0 AND EXISTS (
            SELECT 1 FROM user_fragments uf
            JOIN fragment_words fw ON fw.fragment_id = uf.fragment_id
            WHERE uf.user_id = p_user_id AND fw.id * -1 = tid
          ))
      );
  END IF;

  IF v_displayed_general IS NULL THEN
    SELECT json_agg(elem) INTO v_displayed_general
    FROM (SELECT elem FROM json_array_elements(v_titles_data->'unlockedGeneralTitles') elem LIMIT 1) sub;
  END IF;

  -- Listes de lieux (idem mig 038)
  SELECT COALESCE(json_agg(place_data), '[]'::json) INTO v_authored_places
  FROM (SELECT json_build_object('id', p.id, 'title', p.title, 'type', COALESCE(pt.title, ''), 'createdAt', p.created_at,
    'imageUrl', CASE WHEN p.images IS NOT NULL AND jsonb_array_length(p.images) > 0 THEN p.images->0->>'url' ELSE NULL END
  ) AS place_data FROM places p LEFT JOIN place_types pt ON pt.id = p.place_type_id
  WHERE p.author_id = p_user_id ORDER BY p.created_at DESC LIMIT 500) sub;

  SELECT COALESCE(json_agg(place_data ORDER BY last_visited_at DESC), '[]'::json) INTO v_discovered_places
  FROM (
    SELECT json_build_object(
      'id', p.id, 'title', p.title, 'type', COALESCE(pt.title, ''),
      'imageUrl', CASE WHEN p.images IS NOT NULL AND jsonb_array_length(p.images) > 0 THEN p.images->0->>'url' ELSE NULL END,
      'visitsCount', stats.visits_count,
      'lastVisitedAt', stats.last_visited_at
    ) AS place_data,
    stats.last_visited_at
    FROM (
      SELECT pe.place_id, COUNT(*) AS visits_count, MAX(pe.visited_at) AS last_visited_at
      FROM public.place_explorers pe
      WHERE pe.user_id = p_user_id
      GROUP BY pe.place_id
    ) stats
    JOIN places p ON p.id = stats.place_id
    LEFT JOIN place_types pt ON pt.id = p.place_type_id
    ORDER BY stats.last_visited_at DESC
    LIMIT 500
  ) sub;

  SELECT COALESCE(json_agg(place_data ORDER BY total_points DESC), '[]'::json) INTO v_favorite_places
  FROM (
    SELECT json_build_object(
      'id', p.id, 'title', p.title, 'type', COALESCE(pt.title, ''),
      'imageUrl', CASE WHEN p.images IS NOT NULL AND jsonb_array_length(p.images) > 0 THEN p.images->0->>'url' ELSE NULL END,
      'totalPoints', stats.total_points, 'lastActionAt', stats.last_action_at
    ) AS place_data, stats.total_points
    FROM (
      SELECT al.place_id, SUM((al.data->>'points')::INT) AS total_points, MAX(al.created_at) AS last_action_at
      FROM activity_log al
      WHERE al.actor_id = p_user_id AND al.type = 'place_influence' AND al.place_id IS NOT NULL
      GROUP BY al.place_id ORDER BY total_points DESC LIMIT 50
    ) stats
    JOIN places p ON p.id = stats.place_id
    LEFT JOIN place_types pt ON pt.id = p.place_type_id
  ) sub;

  SELECT COALESCE(json_agg(place_data ORDER BY planted_at DESC), '[]'::json) INTO v_veilled_places
  FROM (
    SELECT json_build_object(
      'id', p.id, 'title', p.title, 'type', COALESCE(pt.title, ''),
      'imageUrl', CASE WHEN p.images IS NOT NULL AND jsonb_array_length(p.images) > 0 THEN p.images->0->>'url' ELSE NULL END,
      'plantedAt', pv.planted_at,
      'memberCount', (SELECT count(*)::int FROM public.expedition_members em2 WHERE em2.expedition_id = pv.expedition_id)
    ) AS place_data, pv.planted_at
    FROM public.place_veille pv
    JOIN public.expedition_members em ON em.expedition_id = pv.expedition_id AND em.user_id = p_user_id
    JOIN public.places p ON p.id = pv.place_id
    LEFT JOIN public.place_types pt ON pt.id = p.place_type_id
    ORDER BY pv.planted_at DESC LIMIT 500
  ) sub;

  SELECT COUNT(DISTINCT place_id) INTO v_lieux_explores FROM public.place_explorers WHERE user_id = p_user_id;
  SELECT COUNT(DISTINCT pv.place_id) INTO v_lieux_veilles
    FROM public.place_veille pv
    JOIN public.expedition_members em ON em.expedition_id = pv.expedition_id
    WHERE em.user_id = p_user_id;
  SELECT COUNT(*) INTO v_enigmas_solved FROM public.enigma_responses WHERE user_id = p_user_id AND correct = TRUE;

  SELECT json_build_object(
    'userId', u.id, 'name', COALESCE(u.first_name, u.email_address),
    'factionId', u.faction_id, 'factionTitle', f.title, 'factionColor', f.color, 'factionPattern', f.pattern,
    'profileImage', u.avatar_url,
    'level', v_level,
    'xpTotal', v_xp_total,
    'xpToNextLevel', v_xp_to_next,
    'xpForNextLevel', v_xp_for_next_level,
    'veteranFirstEra', COALESCE(u.veteran_first_era, false),
    'lieuxExplores', COALESCE(v_lieux_explores, 0),
    'lieuxVeilles',  COALESCE(v_lieux_veilles, 0),
    'enigmasSolved', COALESCE(v_enigmas_solved, 0),
    'discoveredCount', (v_titles_data->'stats'->>'discoveries')::INT,
    'placesAdded', (v_titles_data->'stats'->>'places_added')::INT,
    'carnetsCount', (v_titles_data->'stats'->>'carnets')::INT,
    'plantagesCount', (v_titles_data->'stats'->>'plantages')::INT,
    'joinedAt', u.created_at,
    'displayedGeneralTitles', COALESCE(v_displayed_general, '[]'::json),
    'factionTitle2', v_faction_title,
    'biography', COALESCE(u.bio, u.biography, ''),
    'instagram', u.instagram,
    'authoredPlaces', v_authored_places,
    'discoveredPlaces', v_discovered_places,
    'favoritePlaces', v_favorite_places,
    'veilledPlaces', v_veilled_places,
    'unlockedGeneralTitles', v_titles_data->'unlockedGeneralTitles'
  ) INTO v_result FROM users u LEFT JOIN factions f ON f.id = u.faction_id WHERE u.id = p_user_id;

  RETURN v_result;
END;
$$;

GRANT EXECUTE ON FUNCTION public.get_player_profile(text) TO authenticated, anon, service_role;
```

- [ ] **Step 2: Appliquer + tester sur un user**

```bash
pnpm dlx supabase db push
```

```sql
SELECT public.get_player_profile('<USER_ID_TEST>');
```

Vérifier que le JSON retourne `level`, `xpTotal`, `xpToNextLevel`, `xpForNextLevel`, `veteranFirstEra`. Plus aucune occurrence de `glory`, `explorationPoints`, `eruditionPoints`, `influenceStock`, `influencePlaced` (note : ces 2 derniers étaient retirés à dessein, ils ne sont plus exposés).

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/044_v07_levels_player_profile.sql
git commit -m "feat(v0.7): mig 044 — refonte get_player_profile (level/xp, plus de glory)"
git push
```

---

### Task 6: Migration `045_v07_levels_leaderboard.sql` — refonte get_leaderboard notoriety

**Files:**
- Create: `supabase/migrations/045_v07_levels_leaderboard.sql`

- [ ] **Step 1: Créer le fichier**

```sql
-- 045_v07_levels_leaderboard.sql
-- WHY : refonte de get_leaderboard('notoriety') pour trier par niveau (tie-break xp_total).
-- Les autres types ('authored', 'veilled') restent inchangés.

CREATE OR REPLACE FUNCTION public.get_leaderboard(p_type text, p_limit integer DEFAULT 50)
RETURNS json
LANGUAGE plpgsql SECURITY DEFINER STABLE
AS $$
DECLARE
  v_result json;
BEGIN
  IF p_type = 'notoriety' THEN
    SELECT COALESCE(json_agg(row_data ORDER BY (row_data->>'rank')::int), '[]'::json) INTO v_result
    FROM (
      SELECT json_build_object(
        'rank',         ROW_NUMBER() OVER (ORDER BY u.xp_total DESC, u.id),
        'userId',       u.id,
        'name',         COALESCE(u.display_name, u.first_name, u.email_address),
        'profileImage', u.avatar_url,
        'factionColor', f.color,
        'value',        public._level_from_xp(u.xp_total),
        'xpTotal',      u.xp_total
      ) AS row_data
      FROM public.users u
      LEFT JOIN public.factions f ON f.id = u.faction_id
      WHERE u.xp_total > 0
      ORDER BY u.xp_total DESC, u.id
      LIMIT p_limit
    ) sub;

  ELSIF p_type = 'authored' THEN
    -- Inchangé (mig 038)
    SELECT COALESCE(json_agg(row_data ORDER BY (row_data->>'rank')::int), '[]'::json) INTO v_result
    FROM (
      SELECT json_build_object(
        'rank',         ROW_NUMBER() OVER (ORDER BY COUNT(*) DESC),
        'userId',       u.id,
        'name',         COALESCE(u.display_name, u.first_name, u.email_address),
        'profileImage', u.avatar_url,
        'factionColor', f.color,
        'value',        COUNT(*)::int
      ) AS row_data
      FROM public.users u
      JOIN public.places p ON p.author_id = u.id
      LEFT JOIN public.factions f ON f.id = u.faction_id
      GROUP BY u.id, u.display_name, u.first_name, u.email_address, u.avatar_url, f.color
      ORDER BY COUNT(*) DESC
      LIMIT p_limit
    ) sub;

  ELSIF p_type = 'veilled' THEN
    -- Inchangé (mig 027)
    SELECT COALESCE(json_agg(row_data ORDER BY (row_data->>'rank')::int), '[]'::json) INTO v_result
    FROM (
      SELECT json_build_object(
        'rank',         ROW_NUMBER() OVER (ORDER BY COUNT(DISTINCT pv.place_id) DESC),
        'userId',       u.id,
        'name',         COALESCE(u.display_name, u.first_name, u.email_address),
        'profileImage', u.avatar_url,
        'factionColor', f.color,
        'value',        COUNT(DISTINCT pv.place_id)::int
      ) AS row_data
      FROM public.users u
      JOIN public.expedition_members em ON em.user_id = u.id
      JOIN public.place_veille pv ON pv.expedition_id = em.expedition_id
      LEFT JOIN public.factions f ON f.id = u.faction_id
      GROUP BY u.id, u.display_name, u.first_name, u.email_address, u.avatar_url, f.color
      ORDER BY COUNT(DISTINCT pv.place_id) DESC
      LIMIT p_limit
    ) sub;

  ELSE
    v_result := '[]'::json;
  END IF;

  RETURN v_result;
END;
$$;

GRANT EXECUTE ON FUNCTION public.get_leaderboard(text, integer) TO authenticated, anon, service_role;
```

- [ ] **Step 2: Appliquer + tester**

```bash
pnpm dlx supabase db push
```

```sql
SELECT public.get_leaderboard('notoriety', 5);
```

Note : juste après application, le résultat sera `[]` (aucun user avec xp_total > 0). C'est attendu.

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/045_v07_levels_leaderboard.sql
git commit -m "feat(v0.7): mig 045 — refonte get_leaderboard('notoriety') tri par level"
git push
```

---

### Task 7: Migration `046_v07_levels_action_rpcs.sql` — RPCs d'action enrichies + gating cartographier

**Files:**
- Create: `supabase/migrations/046_v07_levels_action_rpcs.sql`

- [ ] **Step 1: Créer le fichier (helper de réponse + wrapper sur add_place)**

```sql
-- 046_v07_levels_action_rpcs.sql
-- WHY : enrichit les RPCs d'action pour qu'elles retournent {xp_delta, level_before, level_after}.
-- Le front utilise ces infos pour déclencher la modale level up.
-- Plus : gating "cartographier" (= add_place / création de lieu) au niveau 3 minimum.
--
-- Stratégie : on enveloppe les RPCs existantes (claim_place, plant_flag, submit_enigma,
-- add_carnet, add_photo, add_place, discover_place, revisit_place_gps) en lisant
-- xp_total avant et après, puis on ajoute les 3 champs au JSON retourné.

-- Helper : lit xp_total et le niveau correspondant
CREATE OR REPLACE FUNCTION public._user_level_state(p_user_id text)
RETURNS json LANGUAGE sql STABLE AS $$
  SELECT json_build_object(
    'xpTotal', COALESCE(xp_total, 0),
    'level',   public._level_from_xp(COALESCE(xp_total, 0))
  )
  FROM public.users WHERE id = p_user_id;
$$;

GRANT EXECUTE ON FUNCTION public._user_level_state(text) TO authenticated, anon, service_role;

-- ============================================================
-- WRAPPER GENERIC : enrichir un JSON existant avec xp_delta/level_before/level_after
-- Le front lit xp_before / level_before AVANT l'appel, puis recompare APRÈS.
-- (Version simple : le front compare entre 2 lectures via _user_level_state.)
--
-- En réalité le pattern le plus propre : chaque RPC d'action LIT xp_total avant son
-- corps, puis le LIT après son corps, et merge les 3 champs dans son JSON.
-- Pour ne pas reprendre TOUTES les RPCs ici, on suit cette stratégie alternative :
--
-- Le FRONT appelle _user_level_state AVANT et APRÈS chaque action.
-- Avantage : aucune mig de wrapping nécessaire pour TOUTES les RPCs.
-- Inconvénient : 2 round-trips par action (mais déjà le cas pour plusieurs flux).
--
-- Cette mig contient donc UNIQUEMENT le helper _user_level_state.
-- Le wrapping front se fait dans les hooks (Phase 5 du plan).
-- ============================================================

-- ============================================================
-- GATING CARTOGRAPHIER : check niveau >= 3 avant add_place
-- (le nom de la RPC actuelle peut varier — adapter au nom réel)
-- ============================================================

-- D'abord : check si add_place existe
DO $$
DECLARE v_exists boolean;
BEGIN
  SELECT EXISTS (
    SELECT 1 FROM pg_proc p
    JOIN pg_namespace n ON n.oid = p.pronamespace
    WHERE n.nspname = 'public' AND p.proname = 'add_place'
  ) INTO v_exists;
  IF NOT v_exists THEN
    RAISE NOTICE 'add_place RPC introuvable — gating à appliquer manuellement sur la RPC réelle de création de lieu.';
  END IF;
END $$;

-- Helper réutilisable pour le check niveau
CREATE OR REPLACE FUNCTION public._require_min_level(p_user_id text, p_min_level integer)
RETURNS json LANGUAGE plpgsql STABLE AS $$
DECLARE
  v_level integer;
BEGIN
  SELECT public._level_from_xp(COALESCE(xp_total, 0)) INTO v_level
  FROM public.users WHERE id = p_user_id;
  IF v_level IS NULL OR v_level < p_min_level THEN
    RETURN json_build_object('error', 'level_too_low', 'requiredLevel', p_min_level, 'currentLevel', COALESCE(v_level, 1));
  END IF;
  RETURN NULL; -- pas d'erreur, autorisé
END;
$$;

GRANT EXECUTE ON FUNCTION public._require_min_level(text, integer) TO authenticated, anon, service_role;
```

- [ ] **Step 2: Appliquer**

```bash
pnpm dlx supabase db push
```

- [ ] **Step 3: Identifier la (les) RPC(s) réelle(s) de création de lieu**

```sql
SELECT n.nspname || '.' || p.proname AS func_name,
       pg_get_function_arguments(p.oid) AS args
FROM pg_proc p
JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname = 'public'
  AND (p.proname ILIKE '%add%place%' OR p.proname ILIKE '%create%place%');
```

Noter le ou les nom(s) exact(s).

- [ ] **Step 4: Créer un wrapper SQL qui injecte le check niveau dans la RPC trouvée**

(Adapter selon le résultat du Step 3. Exemple si la RPC s'appelle `add_place(p_user_id, p_title, p_lat, p_lng, ...)` :)

```sql
-- Sauvegarde l'original sous un nom interne
ALTER FUNCTION public.add_place(...) RENAME TO _add_place_internal;

-- Crée le wrapper public avec check niveau
CREATE OR REPLACE FUNCTION public.add_place(...)  -- mêmes args que l'original
RETURNS json LANGUAGE plpgsql SECURITY DEFINER AS $$
DECLARE
  v_check_err json;
BEGIN
  v_check_err := public._require_min_level(p_user_id, 3);
  IF v_check_err IS NOT NULL THEN
    RETURN v_check_err;  -- {"error":"level_too_low","requiredLevel":3,"currentLevel":N}
  END IF;
  RETURN public._add_place_internal(...);  -- mêmes args
END;
$$;
GRANT EXECUTE ON FUNCTION public.add_place(...) TO authenticated;
```

- [ ] **Step 5: Test du gating**

```sql
-- Créer un user test avec xp_total = 0 (level 1) ET tenter add_place
SELECT public.add_place(...);  -- doit retourner {"error":"level_too_low",...}

-- Forcer xp_total à 38 (niveau 4) ET réessayer
UPDATE public.users SET xp_total = 38 WHERE id = '<USER_ID_TEST>';
SELECT public.add_place(...);  -- doit fonctionner
```

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/046_v07_levels_action_rpcs.sql
git commit -m "feat(v0.7): mig 046 — helpers level state + gating cartographier (level >= 3)"
git push
```

---

## Phase 5 — Frontend : types, calc, store, hook level

### Task 8: Frontend — types et utils de calcul niveau

**Files:**
- Create: `apps/explore-web/src/types/level.ts`
- Create: `apps/explore-web/src/lib/levelCalc.ts`

- [ ] **Step 1: Créer les types**

```typescript
// apps/explore-web/src/types/level.ts

/** État du niveau du joueur, mirror du JSON exposé par get_player_profile. */
export interface LevelState {
  level: number              // 1..50
  xpTotal: number            // xp cumulée
  xpToNextLevel: number      // xp restante pour atteindre level + 1
  xpForNextLevel: number     // xp totale nécessaire pour level + 1
  veteranFirstEra: boolean   // badge vétéran présent au switch
}

/** Résultat d'une action qui peut faire monter de niveau. */
export interface LevelDelta {
  xpDelta: number            // xp gagnée par l'action
  levelBefore: number        // niveau avant
  levelAfter: number         // niveau après
}

export const LEVEL_CAP = 50
```

- [ ] **Step 2: Créer les fonctions pures de calcul (mirror du SQL)**

```typescript
// apps/explore-web/src/lib/levelCalc.ts

import { LEVEL_CAP } from '../types/level'

/**
 * Calcule le niveau correspondant à une XP cumulée.
 * Doit matcher EXACTEMENT _level_from_xp() en DB :
 *   niv 1 = 0..4 XP, niv 2 = 5..12, niv 3 = 13..37
 *   niv N>=4 = 13 + 500 * (1.05^(N-3) - 1) cumul
 */
export function levelFromXp(xp: number): number {
  if (xp == null || xp < 5) return 1
  if (xp < 13) return 2
  if (xp < 38) return 3
  const computed = 3 + Math.floor(Math.log(1 + (xp - 13) / 500) / Math.log(1.05))
  return Math.min(LEVEL_CAP, computed)
}

/**
 * XP cumulée nécessaire pour ATTEINDRE un niveau N.
 * Mirror de _xp_for_level() en DB.
 */
export function xpForLevel(level: number): number {
  if (level <= 1) return 0
  if (level === 2) return 5
  if (level === 3) return 13
  if (level >= LEVEL_CAP) return 4467
  return Math.round(13 + 500 * (Math.pow(1.05, level - 3) - 1))
}

/** XP restante pour passer au niveau suivant (0 si cap atteint). */
export function xpToNextLevel(currentXp: number): number {
  const lvl = levelFromXp(currentXp)
  if (lvl >= LEVEL_CAP) return 0
  return Math.max(0, xpForLevel(lvl + 1) - currentXp)
}
```

- [ ] **Step 3: Vérifier la cohérence SQL ↔ JS avec quelques cas**

Crée mentalement (ou dans la console node) :

```js
levelFromXp(0)    // 1
levelFromXp(5)    // 2
levelFromXp(13)   // 3
levelFromXp(38)   // 4
levelFromXp(92)   // 6
levelFromXp(1379) // 30
levelFromXp(4467) // 50
levelFromXp(99999) // 50
```

Si une valeur ne match pas le SQL, ajuster.

- [ ] **Step 4: Commit**

```bash
git add apps/explore-web/src/types/level.ts apps/explore-web/src/lib/levelCalc.ts
git commit -m "feat(v0.7): types LevelState/LevelDelta + utils levelFromXp/xpForLevel (mirror SQL)"
git push
```

---

### Task 9: Frontend — étendre playerStore avec niveau

**Files:**
- Modify: `apps/explore-web/src/stores/playerStore.ts`

- [ ] **Step 1: Lire le fichier actuel**

```bash
cat "apps/explore-web/src/stores/playerStore.ts"
```

- [ ] **Step 2: Ajouter les fields niveau dans le store**

Identifier l'interface `PlayerState` et y ajouter :

```typescript
// Dans l'interface PlayerState
level: number
xpTotal: number
xpToNextLevel: number
xpForNextLevel: number
veteranFirstEra: boolean

// Dans les actions du store, ajouter :
setLevelState: (state: { level: number; xpTotal: number; xpToNextLevel: number; xpForNextLevel: number; veteranFirstEra: boolean }) => void
```

- [ ] **Step 3: Implémenter `setLevelState` et défaut**

```typescript
// Dans le create()
level: 1,
xpTotal: 0,
xpToNextLevel: 5,
xpForNextLevel: 5,
veteranFirstEra: false,

setLevelState: (s) => set({
  level: s.level,
  xpTotal: s.xpTotal,
  xpToNextLevel: s.xpToNextLevel,
  xpForNextLevel: s.xpForNextLevel,
  veteranFirstEra: s.veteranFirstEra,
}),
```

- [ ] **Step 4: Vérifier que TypeScript compile**

```bash
cd "C:/Users/uriel/Desktop/DEVS/app (Runes de Chêne)/apps/explore-web"
pnpm exec tsc --noEmit
```

Expected : pas d'erreur de type.

- [ ] **Step 5: Commit**

```bash
git add apps/explore-web/src/stores/playerStore.ts
git commit -m "feat(v0.7): playerStore — ajout level/xpTotal/xpToNextLevel/veteranFirstEra"
git push
```

---

### Task 10: Frontend — hook `useLevel` (lecture)

**Files:**
- Create: `apps/explore-web/src/hooks/useLevel.ts`

- [ ] **Step 1: Créer le hook**

```typescript
// apps/explore-web/src/hooks/useLevel.ts

import { useCallback, useEffect, useState } from 'react'
import { supabase } from '../lib/supabase'
import { usePlayerStore } from '../stores/playerStore'
import type { LevelState } from '../types/level'

/**
 * Lit l'état niveau du joueur via get_player_profile (qui inclut level, xpTotal,
 * xpToNextLevel, xpForNextLevel, veteranFirstEra).
 * Synchronise avec playerStore. Permet refresh manuel après une action.
 */
export function useLevel(autoLoad = true) {
  const userId = usePlayerStore(s => s.userId)
  const setLevelState = usePlayerStore(s => s.setLevelState)
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const refresh = useCallback(async () => {
    if (!userId) return
    setLoading(true); setError(null)
    const { data, error: rpcError } = await supabase.rpc('get_player_profile', { p_user_id: userId })
    setLoading(false)
    if (rpcError) {
      console.error('[useLevel] get_player_profile error:', rpcError.message)
      setError(rpcError.message)
      return
    }
    const profile = data as Record<string, unknown> | null
    if (!profile) return
    setLevelState({
      level: Number(profile.level ?? 1),
      xpTotal: Number(profile.xpTotal ?? 0),
      xpToNextLevel: Number(profile.xpToNextLevel ?? 5),
      xpForNextLevel: Number(profile.xpForNextLevel ?? 5),
      veteranFirstEra: Boolean(profile.veteranFirstEra ?? false),
    })
  }, [userId, setLevelState])

  useEffect(() => {
    if (autoLoad && userId) refresh()
  }, [autoLoad, userId, refresh])

  return { refresh, loading, error }
}
```

- [ ] **Step 2: Vérifier la compilation**

```bash
pnpm exec tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add apps/explore-web/src/hooks/useLevel.ts
git commit -m "feat(v0.7): hook useLevel — lecture niveau via get_player_profile"
git push
```

---

## Phase 6 — Frontend : composants UI

### Task 11: Composant `LevelMedallion` (sceau de chevalier)

**Files:**
- Create: `apps/explore-web/src/components/profile/LevelMedallion.tsx`
- Create: `apps/explore-web/src/components/profile/LevelMedallion.css`

- [ ] **Step 1: Créer le composant TSX**

```typescript
// apps/explore-web/src/components/profile/LevelMedallion.tsx

import './LevelMedallion.css'

interface Props {
  level: number
  size?: 'sm' | 'md' | 'lg'
}

export function LevelMedallion({ level, size = 'md' }: Props) {
  return (
    <div className={`level-medallion level-medallion--${size}`} aria-label={`Niveau ${level}`}>
      <div className="level-medallion__inner">
        <div className="level-medallion__num">{level}</div>
        <div className="level-medallion__lbl">NIV.</div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Créer le CSS (sceau patiné, ocre/sépia)**

```css
/* apps/explore-web/src/components/profile/LevelMedallion.css */

.level-medallion {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, #e8c890, #8a6432 60%, #3a2818);
  border: 3px solid #d4a574;
  box-shadow: 0 2px 8px rgba(0,0,0,0.4), inset 0 1px 2px rgba(255,255,255,0.2);
  flex-shrink: 0;
}
.level-medallion--sm { width: 40px; height: 40px; }
.level-medallion--md { width: 64px; height: 64px; }
.level-medallion--lg { width: 88px; height: 88px; }

.level-medallion__inner {
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
}
.level-medallion__num {
  font-family: 'Alegreya', serif;
  font-weight: 700;
  color: #fff8e0;
  line-height: 1;
}
.level-medallion--sm .level-medallion__num { font-size: 16px; }
.level-medallion--md .level-medallion__num { font-size: 22px; }
.level-medallion--lg .level-medallion__num { font-size: 32px; }
.level-medallion__lbl {
  font-size: 7px;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: rgba(255,248,224,0.7);
  margin-top: 1px;
}
.level-medallion--lg .level-medallion__lbl { font-size: 9px; }
```

- [ ] **Step 3: Vérifier compilation**

```bash
pnpm exec tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add apps/explore-web/src/components/profile/LevelMedallion.tsx apps/explore-web/src/components/profile/LevelMedallion.css
git commit -m "feat(v0.7): composant LevelMedallion (sceau de chevalier, 3 tailles)"
git push
```

---

### Task 12: Composant `VeteranBadge`

**Files:**
- Create: `apps/explore-web/src/components/profile/VeteranBadge.tsx`
- Create: `apps/explore-web/src/components/profile/VeteranBadge.css`

- [ ] **Step 1: Créer le composant**

```typescript
// apps/explore-web/src/components/profile/VeteranBadge.tsx

import './VeteranBadge.css'

interface Props {
  size?: 'sm' | 'md' | 'lg'
}

export function VeteranBadge({ size = 'md' }: Props) {
  return (
    <div
      className={`veteran-badge veteran-badge--${size}`}
      title="Vétéran de la Première Époque"
      aria-label="Vétéran de la Première Époque"
    >
      <span className="veteran-badge__icon">⚜</span>
      <span className="veteran-badge__lbl">Vétéran</span>
    </div>
  )
}
```

- [ ] **Step 2: CSS**

```css
/* apps/explore-web/src/components/profile/VeteranBadge.css */

.veteran-badge {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  padding: 3px 8px;
  background: linear-gradient(135deg, rgba(184, 134, 90, 0.18), rgba(212, 165, 116, 0.08));
  border: 1px solid rgba(212, 165, 116, 0.5);
  border-radius: 12px;
  color: #d4a574;
  font-family: 'Alegreya', serif;
  font-style: italic;
  letter-spacing: 0.5px;
  white-space: nowrap;
}
.veteran-badge--sm { font-size: 10px; padding: 2px 6px; }
.veteran-badge--md { font-size: 12px; padding: 3px 8px; }
.veteran-badge--lg { font-size: 14px; padding: 4px 10px; }
.veteran-badge__icon {
  font-size: 1.1em;
  line-height: 1;
}
```

- [ ] **Step 3: Compile + commit**

```bash
pnpm exec tsc --noEmit
git add apps/explore-web/src/components/profile/VeteranBadge.tsx apps/explore-web/src/components/profile/VeteranBadge.css
git commit -m "feat(v0.7): composant VeteranBadge (Vétéran de la Première Époque)"
git push
```

---

### Task 13: Composant `GloryProgressBar`

**Files:**
- Create: `apps/explore-web/src/components/profile/GloryProgressBar.tsx`
- Create: `apps/explore-web/src/components/profile/GloryProgressBar.css`

- [ ] **Step 1: Composant**

```typescript
// apps/explore-web/src/components/profile/GloryProgressBar.tsx

import './GloryProgressBar.css'

interface Props {
  xpTotal: number
  xpForNextLevel: number   // xp totale nécessaire pour atteindre level+1
  xpToNextLevel: number    // xp restante avant level+1
  level: number
}

export function GloryProgressBar({ xpTotal, xpForNextLevel, xpToNextLevel, level }: Props) {
  const isCap = level >= 50
  const progressFromCurrentLevel = isCap ? 100 : Math.min(100, Math.max(0, ((xpForNextLevel - xpToNextLevel) / xpForNextLevel) * 100))
  // Note: progress visuel approximatif. Pour précision, on pourrait calculer
  // (xpTotal - xpForLevel(level)) / (xpForLevel(level+1) - xpForLevel(level)) * 100

  return (
    <div className="glory-bar">
      <div className="glory-bar__track">
        <div className="glory-bar__fill" style={{ width: `${progressFromCurrentLevel}%` }} />
      </div>
      <div className="glory-bar__text">
        {isCap
          ? 'Cap atteint — tu es Légende.'
          : <><strong>{xpToNextLevel}</strong> Gloire avant le prochain niveau</>
        }
      </div>
    </div>
  )
}
```

- [ ] **Step 2: CSS**

```css
/* apps/explore-web/src/components/profile/GloryProgressBar.css */

.glory-bar { display: flex; flex-direction: column; gap: 4px; width: 100%; }
.glory-bar__track {
  background: rgba(255, 255, 255, 0.06);
  border-radius: 4px;
  height: 8px;
  overflow: hidden;
}
.glory-bar__fill {
  background: linear-gradient(90deg, #b8865a, #d4a574);
  height: 100%;
  transition: width 0.4s ease-out;
}
.glory-bar__text {
  font-size: 12px;
  opacity: 0.75;
  font-family: 'Alegreya', serif;
}
```

- [ ] **Step 3: Compile + commit**

```bash
pnpm exec tsc --noEmit
git add apps/explore-web/src/components/profile/GloryProgressBar.tsx apps/explore-web/src/components/profile/GloryProgressBar.css
git commit -m "feat(v0.7): composant GloryProgressBar (X Gloire avant prochain niveau)"
git push
```

---

### Task 14: Modale `LevelUpModal` (single + multi-niveau)

**Files:**
- Create: `apps/explore-web/src/components/levelup/LevelUpModal.tsx`
- Create: `apps/explore-web/src/components/levelup/LevelUpModal.css`

- [ ] **Step 1: Composant**

```typescript
// apps/explore-web/src/components/levelup/LevelUpModal.tsx

import { useEffect, useState } from 'react'
import './LevelUpModal.css'

interface Props {
  levelBefore: number
  levelAfter: number
  onClose: () => void
}

const QUOTES_BY_LEVEL: Record<number, string> = {
  5: 'Tu as gagné ton premier vrai palier, Compagnon.',
  10: 'Dix paliers gravis. La marche te porte.',
  15: 'Te voilà Veilleur, dans le sens fort du mot.',
  20: 'Vingt niveaux. Ton nom commence à porter.',
  25: 'Tu es désormais un Héros local.',
  30: 'Tu es désormais un Héros régional. Ton renom dépasse ta vallée.',
  40: 'Quarante paliers. Peu de Veilleurs y arrivent.',
  42: 'Tu portes désormais le titre de Héros.',
  50: 'Légende. Le sommet est à toi.',
}

export function LevelUpModal({ levelBefore, levelAfter, onClose }: Props) {
  const isMulti = levelAfter - levelBefore > 1
  const [displayLevel, setDisplayLevel] = useState(levelBefore)

  useEffect(() => {
    if (!isMulti) {
      setDisplayLevel(levelAfter)
      return
    }
    // Animation : compte de levelBefore à levelAfter en 800ms
    const startTime = Date.now()
    const duration = 800
    const tick = () => {
      const t = Math.min(1, (Date.now() - startTime) / duration)
      const v = Math.round(levelBefore + (levelAfter - levelBefore) * t)
      setDisplayLevel(v)
      if (t < 1) requestAnimationFrame(tick)
    }
    requestAnimationFrame(tick)
  }, [isMulti, levelBefore, levelAfter])

  const quote = QUOTES_BY_LEVEL[levelAfter] ?? 'Ta gloire grandit, Veilleur.'

  return (
    <div className="lvlup-overlay" onClick={onClose}>
      <div className="lvlup-modal" onClick={(e) => e.stopPropagation()}>
        <div className="lvlup-label">
          {isMulti ? 'Plusieurs paliers d\'un coup' : 'Tu as gagné un palier'}
        </div>
        <div className="lvlup-num">
          {isMulti
            ? <>Niveau <span>{displayLevel}</span></>
            : <>Niveau {levelAfter}</>
          }
        </div>
        <div className="lvlup-quote">« {quote} »</div>
        <button className="lvlup-btn" onClick={onClose}>Continuer</button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: CSS**

```css
/* apps/explore-web/src/components/levelup/LevelUpModal.css */

.lvlup-overlay {
  position: fixed; inset: 0;
  background: rgba(0, 0, 0, 0.7);
  display: flex; align-items: center; justify-content: center;
  z-index: 9999;
  animation: lvlup-fade-in 0.3s ease-out;
}
@keyframes lvlup-fade-in {
  from { opacity: 0; } to { opacity: 1; }
}

.lvlup-modal {
  background: radial-gradient(ellipse at top, #3a2818, #1a1612);
  border: 2px solid #d4a574;
  padding: 28px 36px;
  border-radius: 10px;
  color: #e8dfc8;
  text-align: center;
  box-shadow: 0 4px 24px rgba(212, 165, 116, 0.3);
  max-width: 90vw;
  font-family: 'Alegreya', serif;
  animation: lvlup-scale-in 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
}
@keyframes lvlup-scale-in {
  from { transform: scale(0.85); opacity: 0; }
  to   { transform: scale(1);    opacity: 1; }
}

.lvlup-label {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 2px;
  opacity: 0.7;
  margin-bottom: 8px;
}
.lvlup-num {
  font-size: 48px;
  font-weight: 700;
  color: #d4a574;
  margin: 8px 0;
  letter-spacing: -1px;
}
.lvlup-num span {
  display: inline-block;
  min-width: 1.2ch;
}
.lvlup-quote {
  font-size: 15px;
  font-style: italic;
  opacity: 0.85;
  margin: 8px 0 16px;
  max-width: 380px;
}
.lvlup-btn {
  background: #d4a574;
  color: #1a1612;
  border: none;
  padding: 10px 24px;
  border-radius: 6px;
  font-family: inherit;
  font-weight: 600;
  font-size: 15px;
  cursor: pointer;
  transition: background 0.2s;
}
.lvlup-btn:hover { background: #e8c890; }
```

- [ ] **Step 3: Compile + commit**

```bash
pnpm exec tsc --noEmit
git add apps/explore-web/src/components/levelup/
git commit -m "feat(v0.7): modale LevelUpModal (single + multi-niveau animé, citations par palier)"
git push
```

---

### Task 15: Modale `VeteranWelcomeModal` (premier login post-switch)

**Files:**
- Create: `apps/explore-web/src/components/levelup/VeteranWelcomeModal.tsx`
- Create: `apps/explore-web/src/components/levelup/VeteranWelcomeModal.css`

- [ ] **Step 1: Composant**

```typescript
// apps/explore-web/src/components/levelup/VeteranWelcomeModal.tsx

import './VeteranWelcomeModal.css'
import { VeteranBadge } from '../profile/VeteranBadge'

interface Props {
  onClose: () => void
}

export function VeteranWelcomeModal({ onClose }: Props) {
  return (
    <div className="veteran-modal-overlay" onClick={onClose}>
      <div className="veteran-modal" onClick={(e) => e.stopPropagation()}>
        <div className="veteran-modal__title">Une nouvelle ère commence.</div>
        <div className="veteran-modal__badge">
          <VeteranBadge size="lg" />
        </div>
        <p className="veteran-modal__body">
          Tu étais là avant. Le badge <strong>Vétéran de la Première Époque</strong> est désormais gravé sur ton profil — il restera à vie.
        </p>
        <p className="veteran-modal__body veteran-modal__body--sub">
          Tous les Veilleurs reprennent depuis le Niveau 1. Vos accomplissements antérieurs sont reconnus — par votre badge, et par notre mémoire.
        </p>
        <button className="veteran-modal__btn" onClick={onClose}>Reprendre la marche</button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: CSS**

```css
/* apps/explore-web/src/components/levelup/VeteranWelcomeModal.css */

.veteran-modal-overlay {
  position: fixed; inset: 0;
  background: rgba(0, 0, 0, 0.85);
  display: flex; align-items: center; justify-content: center;
  z-index: 10000;
}
.veteran-modal {
  background: radial-gradient(ellipse at top, #3a2818, #0d0a06);
  border: 2px solid #d4a574;
  padding: 32px 40px;
  border-radius: 10px;
  color: #e8dfc8;
  text-align: center;
  max-width: 92vw;
  width: 480px;
  font-family: 'Alegreya', serif;
}
.veteran-modal__title {
  font-size: 22px;
  font-style: italic;
  color: #d4a574;
  margin-bottom: 18px;
}
.veteran-modal__badge {
  display: flex;
  justify-content: center;
  margin: 12px 0 18px;
  transform: scale(1.4);
}
.veteran-modal__body {
  font-size: 15px;
  line-height: 1.55;
  opacity: 0.92;
  margin: 12px 0;
}
.veteran-modal__body--sub {
  font-size: 13px;
  opacity: 0.7;
}
.veteran-modal__btn {
  background: #d4a574;
  color: #1a1612;
  border: none;
  padding: 12px 28px;
  border-radius: 6px;
  font-family: inherit;
  font-weight: 600;
  font-size: 15px;
  margin-top: 12px;
  cursor: pointer;
}
.veteran-modal__btn:hover { background: #e8c890; }
```

- [ ] **Step 3: Commit**

```bash
pnpm exec tsc --noEmit
git add apps/explore-web/src/components/levelup/VeteranWelcomeModal.tsx apps/explore-web/src/components/levelup/VeteranWelcomeModal.css
git commit -m "feat(v0.7): modale VeteranWelcomeModal (premier login post-switch)"
git push
```

---

### Task 16: Hook `useLevelUp` — déclenchement modale level up

**Files:**
- Create: `apps/explore-web/src/hooks/useLevelUp.ts`

- [ ] **Step 1: Créer le hook qui surveille les changements de niveau et expose la modale**

```typescript
// apps/explore-web/src/hooks/useLevelUp.ts

import { useEffect, useRef, useState } from 'react'
import { usePlayerStore } from '../stores/playerStore'

interface LevelUpEvent {
  levelBefore: number
  levelAfter: number
}

/**
 * Surveille les changements de level dans playerStore.
 * Quand level augmente (suite à un refresh post-action), expose un event
 * pour que App.tsx affiche la LevelUpModal.
 *
 * Usage :
 *   const { pendingLevelUp, dismiss } = useLevelUp()
 *   {pendingLevelUp && <LevelUpModal {...pendingLevelUp} onClose={dismiss} />}
 */
export function useLevelUp() {
  const level = usePlayerStore(s => s.level)
  const previousLevelRef = useRef<number>(level)
  const [pendingLevelUp, setPendingLevelUp] = useState<LevelUpEvent | null>(null)

  useEffect(() => {
    const previous = previousLevelRef.current
    if (level > previous) {
      setPendingLevelUp({ levelBefore: previous, levelAfter: level })
    }
    previousLevelRef.current = level
  }, [level])

  const dismiss = () => setPendingLevelUp(null)

  return { pendingLevelUp, dismiss }
}
```

- [ ] **Step 2: Compile + commit**

```bash
pnpm exec tsc --noEmit
git add apps/explore-web/src/hooks/useLevelUp.ts
git commit -m "feat(v0.7): hook useLevelUp — déclenche modale au passage de niveau"
git push
```

---

### Task 17: Refonte des toasts dans `usePlayer.ts` (lever le bug + nouveaux wordings)

**Files:**
- Modify: `apps/explore-web/src/hooks/usePlayer.ts`

- [ ] **Step 1: Lire le fichier complet**

```bash
cat "apps/explore-web/src/hooks/usePlayer.ts" | head -350
```

Identifier les blocs :
- Toast `discover` (~ligne 325-330)
- Fonction qui appelle `discover_place` et affiche le toast (~ligne 595-612)
- Toast `enigma` (~ligne 378)
- Autres toasts d'action

- [ ] **Step 2: Remplacer les wordings selon les nouveaux libellés (cf. spec §7.2)**

Pour chaque toast d'action, adapter le `message` au tableau de spec :

| Action | Avant | Après |
|---|---|---|
| Découvrir (remote, sans visite) | `'Vous avez découvert ${place} 🧭 +1 Gloire +1 Coupe'` | `'🔍 Le brouillard se lève sur ${place} +1 Gloire'` |
| Découvrir + Fouler simultanés (visite GPS d'un lieu jamais découvert) | `'Nouveau lieu découvert ! 🎖️ +1 Gloire 🏆 +1 Coupe'` | `'🥾 Tu as découvert et foulé ${place} +4 Gloire / +3 Coupe'` |
| Fouler (visite GPS d'un lieu déjà découvert) | (n'existait peut-être pas distinct) | `'🥾 Tu as foulé ${place} +3 Gloire / +3 Coupe'` |
| Énigme résolue | `'Énigme résolue ! 📖 +${gain} Gloire +${gain} Coupe +1 énigme validée'` | `'🦉 Énigme résolue sur ${place} +${gain} Gloire / +${gain} Coupe'` |
| Plantage de bannière | (variable selon impl actuelle) | `'🏴 Tu as planté ta bannière à ${place} +10 Gloire / +10 Coupe'` |
| Carnet | (variable) | `'✍️ Tu as écrit un récit sur ${place} +5 Gloire / +5 Coupe'` |
| Photo | (variable) | `'📷 Tu as ajouté une photo de ${place} +1 Gloire / +1 Coupe'` |
| Cartographie | (variable) | `'📜 Tu as cartographié ${place} +20 Gloire / +20 Coupe'` |

- [ ] **Step 3: Logique de distinction "découvert seul" vs "découvert + foulé" vs "foulé seul"**

Côté code, la distinction se fait sur la `method` retournée par `discover_place` (et la présence d'un INSERT dans `place_explorers`). Si la RPC ne retourne pas explicitement la méthode, la déterminer côté front via :
- Si la position GPS du joueur est à moins de X mètres du lieu → "foulé"
- Sinon → "découvert seul"

À défaut de cette info, utiliser le wording neutre "découvert" pour tout appel à `discover_place` et "foulé" uniquement quand on appelle explicitement `revisit_place_gps` ou que le retour confirme une visite physique.

- [ ] **Step 4: Après refresh post-action, déclencher useLevel().refresh() pour que useLevelUp détecte le changement**

Identifier les fonctions qui appellent les RPCs d'action et après chaque succès, appeler le refresh du niveau. Exemple :

```typescript
import { useLevel } from './useLevel'

// Dans les fonctions d'action :
const { refresh: refreshLevel } = useLevel(false)  // false = pas d'autoload

// Après le succès d'une action :
await refreshLevel()
```

- [ ] **Step 5: Compile + test manuel via dev**

```bash
pnpm exec tsc --noEmit
pnpm dev
```

Ouvrir http://localhost:3000, se connecter, tester :
- Découvrir un lieu à distance → vérifier toast "Le brouillard se lève sur..."
- Si le compteur xp_total bouge réellement (vérifiable en SQL ou via profil)

- [ ] **Step 6: Commit**

```bash
git add apps/explore-web/src/hooks/usePlayer.ts
git commit -m "fix(v0.7): toasts honnêtes (résout bug +1 Coupe trompeur sur découverte distance) + nouveaux wordings"
git push
```

---

### Task 18: Intégration médaillon + badge + barre dans le PlayerProfilePanel

**Files:**
- Modify: chemin du PlayerProfilePanel (à identifier — typiquement dans `apps/explore-web/src/components/auth/` ou `map/` ou `players/`)

- [ ] **Step 1: Identifier le fichier qui affiche le profil joueur (ou le sien)**

```bash
grep -rn "displayName\|profileImage\|first_name" "apps/explore-web/src/components" -l | head -5
```

Trouver le composant qui rend les infos profil principales.

- [ ] **Step 2: Ajouter les imports**

```typescript
import { LevelMedallion } from '../profile/LevelMedallion'
import { VeteranBadge } from '../profile/VeteranBadge'
import { GloryProgressBar } from '../profile/GloryProgressBar'
import { usePlayerStore } from '../../stores/playerStore'
```

- [ ] **Step 3: Insérer le médaillon + badge + barre dans le bandeau d'identité**

```tsx
const level = usePlayerStore(s => s.level)
const xpTotal = usePlayerStore(s => s.xpTotal)
const xpToNextLevel = usePlayerStore(s => s.xpToNextLevel)
const xpForNextLevel = usePlayerStore(s => s.xpForNextLevel)
const veteranFirstEra = usePlayerStore(s => s.veteranFirstEra)

// Dans le JSX :
<div className="profile-header">
  <Avatar src={profileImage} />
  <div className="profile-header__main">
    <div className="profile-header__name">{name}</div>
    {veteranFirstEra && <VeteranBadge size="sm" />}
    <GloryProgressBar
      level={level}
      xpTotal={xpTotal}
      xpForNextLevel={xpForNextLevel}
      xpToNextLevel={xpToNextLevel}
    />
  </div>
  <LevelMedallion level={level} size="md" />
</div>
```

- [ ] **Step 4: Retirer l'affichage de la Gloire brute (si présente)**

Chercher les `glory`, `Gloire :`, etc. dans le composant et les supprimer ou remplacer par la barre de progression seule.

- [ ] **Step 5: Compile + test visuel via dev server**

```bash
pnpm exec tsc --noEmit
pnpm dev
```

Vérifier visuellement :
- Médaillon visible à droite de l'avatar
- Badge Vétéran visible si user a `veteran_first_era = true`
- Barre de progression sous le nom
- Plus aucun "+ N Gloire" sec affiché

- [ ] **Step 6: Commit**

```bash
git add apps/explore-web/src/components/path/to/PlayerProfilePanel.tsx
git commit -m "feat(v0.7): profile — intégration médaillon niveau + badge vétéran + barre progression Gloire"
git push
```

---

### Task 19: Intégration des modales dans App.tsx

**Files:**
- Modify: `apps/explore-web/src/App.tsx` (ou le composant racine concerné)

- [ ] **Step 1: Importer les hooks et modales**

```typescript
import { useLevelUp } from './hooks/useLevelUp'
import { LevelUpModal } from './components/levelup/LevelUpModal'
import { VeteranWelcomeModal } from './components/levelup/VeteranWelcomeModal'
import { usePlayerStore } from './stores/playerStore'
```

- [ ] **Step 2: Logique de premier login pour vétérans (avec localStorage)**

```typescript
const veteranFirstEra = usePlayerStore(s => s.veteranFirstEra)
const userId = usePlayerStore(s => s.userId)
const [showVeteranWelcome, setShowVeteranWelcome] = useState(false)

useEffect(() => {
  if (!userId || !veteranFirstEra) return
  const seenKey = `veteranWelcomeSeen_${userId}`
  if (!localStorage.getItem(seenKey)) {
    setShowVeteranWelcome(true)
    localStorage.setItem(seenKey, '1')
  }
}, [userId, veteranFirstEra])

const { pendingLevelUp, dismiss } = useLevelUp()

// Dans le JSX racine :
{pendingLevelUp && (
  <LevelUpModal
    levelBefore={pendingLevelUp.levelBefore}
    levelAfter={pendingLevelUp.levelAfter}
    onClose={dismiss}
  />
)}
{showVeteranWelcome && (
  <VeteranWelcomeModal onClose={() => setShowVeteranWelcome(false)} />
)}
```

- [ ] **Step 3: Compile + test via dev**

```bash
pnpm exec tsc --noEmit
pnpm dev
```

Test manuel :
- Vider le localStorage manuellement (`localStorage.clear()`) puis recharger en étant un vétéran
- Vérifier que VeteranWelcomeModal apparaît
- Faire une action qui passe un niveau (forcer xp_total à 5 dans Supabase, recharger profil) → LevelUpModal apparaît

- [ ] **Step 4: Commit**

```bash
git add apps/explore-web/src/App.tsx
git commit -m "feat(v0.7): App — intégration LevelUpModal + VeteranWelcomeModal (1er login vétéran)"
git push
```

---

### Task 20: Gating UI "Cartographier" si niveau < 3

**Files:**
- Modify: `apps/explore-web/src/components/places/AddPlaceFlow.tsx` (ou bouton équivalent)

- [ ] **Step 1: Identifier où le bouton "Ajouter un lieu" est affiché**

```bash
grep -rn "AddPlace\|Ajouter un lieu\|Cartographier" "apps/explore-web/src/components" -l | head -5
```

- [ ] **Step 2: Ajouter le check niveau pour griser le bouton et afficher un tooltip explicatif**

```tsx
const level = usePlayerStore(s => s.level)
const xpTotal = usePlayerStore(s => s.xpTotal)
const xpForLevel3 = 13  // import xpForLevel from '../../lib/levelCalc' si on veut être propre
const canCartographier = level >= 3
const remainingForUnlock = Math.max(0, xpForLevel3 - xpTotal)

// Bouton :
<button
  disabled={!canCartographier}
  onClick={() => {
    if (!canCartographier) {
      setShowGatedModal(true)
      return
    }
    openAddPlaceFlow()
  }}
  title={canCartographier ? '' : `Réservé aux Veilleurs niveau 3 et plus (encore ${remainingForUnlock} Gloire)`}
>
  {canCartographier ? '📜 Cartographier' : '🔒 Cartographier'}
</button>

{showGatedModal && (
  <div className="gated-modal-overlay" onClick={() => setShowGatedModal(false)}>
    <div className="gated-modal" onClick={(e) => e.stopPropagation()}>
      <p>L'ajout de lieux est réservé aux Veilleurs de niveau 3 et plus. Continue d'explorer pour le débloquer.</p>
      <p>Plus que <strong>{remainingForUnlock} Gloire</strong> avant le niveau 3.</p>
      <button onClick={() => setShowGatedModal(false)}>OK</button>
    </div>
  </div>
)}
```

- [ ] **Step 3: Côté serveur, le check est déjà en place via mig 046 (RPC `add_place` retourne `{error: 'level_too_low'}` si niveau < 3). Vérifier que le front gère ce cas gracieusement.**

```typescript
// Après l'appel RPC add_place :
if (data?.error === 'level_too_low') {
  setError('Tu dois être au moins niveau 3 pour cartographier un lieu.')
  return
}
```

- [ ] **Step 4: Test via dev avec un user niveau < 3 puis un niveau >= 3**

- [ ] **Step 5: Commit**

```bash
git add apps/explore-web/src/components/places/
git commit -m "feat(v0.7): gating cartographier — bouton grisé + modale explicative si niveau < 3"
git push
```

---

## Phase 7 — Migration vétéran et cleanup

### Task 21: Migration `047_v07_levels_veteran_marking.sql`

**Files:**
- Create: `supabase/migrations/047_v07_levels_veteran_marking.sql`

**À appliquer JUSTE AVANT le déploiement front (fenêtre courte entre cette mig et le push front).**

- [ ] **Step 1: Créer le fichier**

```sql
-- 047_v07_levels_veteran_marking.sql
-- WHY : marque comme "Vétéran de la Première Époque" tous les users qui ont
-- contribué au moins une fois avant le déploiement de la feature niveaux.
--
-- À APPLIQUER JUSTE AVANT le déploiement front pour minimiser la fenêtre où
-- de nouveaux contributeurs (post-mig 040 mais pré-deploy front) seraient
-- considérés comme vétérans alors qu'ils n'auraient connu que le système niveaux.

UPDATE public.users SET veteran_first_era = true
WHERE id IN (
  SELECT DISTINCT user_id FROM public.place_explorers WHERE visited_at < public._xp_epoch()
  UNION
  SELECT author_id FROM public.places WHERE author_id IS NOT NULL AND created_at < public._xp_epoch()
  UNION
  SELECT user_id FROM public.place_contributions WHERE created_at < public._xp_epoch()
  UNION
  SELECT user_id FROM public.enigma_responses WHERE correct = true AND responded_at < public._xp_epoch()
  UNION
  SELECT user_id FROM public.veille_history WHERE planted_at < public._xp_epoch()
  UNION
  SELECT user_id FROM public.places_discovered WHERE created_at < public._xp_epoch()
);

-- Note du nombre de vétérans pour communication :
SELECT COUNT(*) AS vetus_marked FROM public.users WHERE veteran_first_era = true;
```

- [ ] **Step 2: Appliquer**

```bash
pnpm dlx supabase db push
```

- [ ] **Step 3: Vérifier le décompte des vétérans**

```sql
SELECT COUNT(*) AS total_users FROM public.users;
SELECT COUNT(*) AS vets FROM public.users WHERE veteran_first_era = true;
```

Le ratio doit refléter la proportion de joueurs ayant déjà contribué.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/047_v07_levels_veteran_marking.sql
git commit -m "feat(v0.7): mig 047 — marquage vétérans (pré-deploy front, fenêtre courte)"
git push
```

---

## Phase 8 — Documentation & rollout

### Task 22: Mise à jour `apps/explore-web/CLAUDE.md`

**Files:**
- Modify: `apps/explore-web/CLAUDE.md`

- [ ] **Step 1: Ajouter une section "Système niveaux V0.7"**

Insérer après "Spécificités cette app" :

```markdown
## Système Niveaux (V0.7, mai 2026)

- Compteur central : `users.xp_total` (jamais affiché brut au joueur).
- Niveau dérivé : function SQL `_level_from_xp(xp_total)`, cap 50.
- Mot lyrique pour l'XP côté UI : **Gloire**.
- Triggers DB sur `places_discovered`, `place_explorers`, `places`, `place_contributions`, `veille_history`, `enigma_responses` — incrémentent / décrémentent `xp_total` (avec garde `xp_epoch` pour ignorer le pré-switch).
- Vocabulaire métier (UI joueur vs interne) :
  - **Découvrir** (interne) = "Le brouillard se lève sur..." (UI) — `places_discovered` INSERT
  - **Fouler** = "Tu as foulé..." — `place_explorers` INSERT
  - **Cartographier** = "Tu as cartographié..." — `places` INSERT (gating niveau 3 minimum)
- Spec design : `docs/superpowers/specs/2026-05-01-v07-niveaux-design.md`
- Plan implémentation : `docs/superpowers/plans/2026-05-01-v07-niveaux.md`
```

- [ ] **Step 2: Commit**

```bash
git add apps/explore-web/CLAUDE.md
git commit -m "docs(v0.7): explore-web/CLAUDE.md — mention système niveaux + vocabulaire"
git push
```

---

### Task 23: Préparer le changelog public et l'email vétérans

**Files:**
- Identifier où vivent les changelogs (vérifier `apps/explore-web/src/components/` ou dossier `docs/`)

- [ ] **Step 1: Localiser le système de changelog actuel**

```bash
grep -rn "changelog\|Changelog" "apps/explore-web/src" -l | head -5
ls "apps/explore-web/src/data/" 2>/dev/null
```

- [ ] **Step 2: Rédiger une entrée changelog (texte exact)**

Ajouter une nouvelle entrée datée du jour de déploiement :

```markdown
## V0.7 — Niveaux (date du déploiement)

Le système d'expérience évolue. Tous les Veilleurs reprennent depuis le **Niveau 1**, mais ceux qui étaient là avant gardent à vie le badge **Vétéran de la Première Époque**.

Vos accomplissements antérieurs sont reconnus — par votre badge, et par notre mémoire. À partir d'aujourd'hui, chaque pas compte.

**Nouveautés mécaniques :**
- Trois actions désormais distinctes : **Découvrir** (sortir un lieu du brouillard), **Fouler** (visite GPS physique), **Cartographier** (créer un nouveau lieu).
- La création de lieu est réservée aux Veilleurs de niveau 3 et plus.
- Les titres généraux ont été refondus : 33 paliers acquis à vie sur 7 axes thématiques.
- Plus aucun titre "perdu" en se faisant doubler — vos titres sont gravés une fois atteints.

Bonne marche.
```

- [ ] **Step 3: Préparer l'email vétérans (à envoyer manuellement le jour J)**

Ouvrir `email-generator/` ou doc équivalent. Sujet et corps suggérés :

> **Sujet :** Une nouvelle ère pour Runes de Chêne
>
> Cher Veilleur,
>
> Une nouvelle version de l'application est en ligne. Le système d'expérience évolue : à partir d'aujourd'hui, chaque pas que tu poses fait grandir ton **niveau de Veilleur** (cap à 50, atteignable au fil des années).
>
> Tu reprends ta marche depuis le Niveau 1, comme tout le monde — mais le badge **"Vétéran de la Première Époque"** est désormais gravé sur ton profil, à vie. Il n'appartient qu'à ceux qui étaient là avant ce changement.
>
> Quelques paliers à viser :
> - **Niveau 3** : tu débloques la cartographie (création de lieux).
> - **Niveau 30** : tu portes le titre de Héros régional.
> - **Niveau 50** : tu deviens Légende.
>
> Continue d'explorer.

- [ ] **Step 4: Commit du changelog**

```bash
git add apps/explore-web/src/data/changelog.ts  # ou le fichier équivalent
git commit -m "docs(v0.7): changelog public — annonce système niveaux"
git push
```

---

### Task 24: Migration différée `048_v07_drop_glory_legacy.sql`

**Files:**
- Create: `supabase/migrations/048_v07_drop_glory_legacy.sql`

**À appliquer ~2 SEMAINES APRÈS le déploiement, une fois confirmation que rien n'est cassé.**

- [ ] **Step 1: Créer le fichier (sans l'appliquer)**

```sql
-- 048_v07_drop_glory_legacy.sql
-- WHY : cleanup différé des entités liées à la Gloire (avant V0.7).
-- À appliquer ~2 semaines après le déploiement de la mig 040+, une fois
-- confirmation que plus rien ne dépend de get_my_glory ni des colonnes
-- exploration_points / erudition_points.

-- 1. DROP de la RPC get_my_glory (remplacée par get_player_profile)
DROP FUNCTION IF EXISTS public.get_my_glory(text);

-- 2. DROP des colonnes legacy V0.5
ALTER TABLE public.users
  DROP COLUMN IF EXISTS exploration_points,
  DROP COLUMN IF EXISTS erudition_points;

-- 3. _enigma_score_weighted reste en place (utilisé par get_coupe_state pour la saison)
```

- [ ] **Step 2: Ne PAS appliquer immédiatement. Commit le fichier seul.**

```bash
git add supabase/migrations/048_v07_drop_glory_legacy.sql
git commit -m "chore(v0.7): mig 048 — cleanup glory legacy (À APPLIQUER MANUELLEMENT ~2 SEM POST-DEPLOY)"
git push
```

- [ ] **Step 3: Créer un rappel calendrier (manuel hors plan)**

Ajouter un TODO dans `docs/superpowers/plans/` ou `CLAUDE.md` pour rappeler d'appliquer cette mig dans 2 semaines après le go live.

---

## Self-Review Checklist (XO interne, à exécuter avant handoff)

- [ ] **Spec coverage** : chaque section §1 à §10 du spec a au moins une task qui l'implémente. Vérifié :
  - §1 motivation → couvert par toutes les tasks
  - §1.bis vocabulaire → Task 17, Task 22
  - §2 décisions → couvertes globalement
  - §3 architecture → Task 1 (schéma), Task 2 (triggers), Task 7 (helper level state)
  - §4 calibration → Task 1 (functions SQL), Task 8 (mirror JS)
  - §5 coefficients → Task 2 (triggers), Task 17 (toasts)
  - §6 migration switch → Task 1, Task 21
  - §7 UI/UX → Tasks 11-20
  - §8 titres → Task 3 (refonte), Task 4 (get_user_titles)
  - §9 cas limites → Task 2 (GREATEST(0,...)), Task 7 (gating)
  - §10 hors scope → mentionné dans le plan (Tasks 24 pour cleanup différé)

- [ ] **Placeholders** : aucun "TBD"/"TODO"/"à implémenter" laissé dans les steps.

- [ ] **Type consistency** : `level`, `xpTotal`, `xpToNextLevel`, `xpForNextLevel`, `veteranFirstEra` utilisés cohéremment entre `get_player_profile` (Task 5), types TS (Task 8), playerStore (Task 9), useLevel (Task 10), composants (Tasks 11-13), modales (Tasks 14-15), hook useLevelUp (Task 16). Vérifié.

- [ ] **Nommage SQL** : `xp_total`, `xp_epoch`, `_level_from_xp`, `_xp_for_level`, `veteran_first_era` cohérents partout.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-01-v07-niveaux.md`. Two execution options:

**1. Subagent-Driven (recommended)** — Je dispatche un fresh subagent par task, review entre chaque, itération rapide. Bon pour ce plan car beaucoup de SQL à vérifier individuellement.

**2. Inline Execution** — J'exécute les tasks dans cette session avec `executing-plans` skill, batch execution avec checkpoints à tes moments-clés.

**Quelle approche tu veux ?**
