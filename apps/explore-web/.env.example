# Variables d'environnement explore-web
# Copier vers .env.local et remplir avec les vraies valeurs (gitignored)

# VAPID public key pour Web Push API (côté client uniquement, public par design)
VITE_VAPID_PUBLIC_KEY=
